[package]
name = "portal-flasher"
description = "Production SWD flashing rig for KC79 Portal boards."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "portal-flasher"
path = "src/main.rs"

[dependencies]
# Matching av-gui-host's own axum, so the router this app merges in is the same type.
axum.workspace = true
serde.workspace = true

av-gui-bus.workspace = true
av-gui-host.workspace = true
av-gui-shell.workspace = true
av-operator-app.workspace = true
portal-swd = { path = "../portal-swd" }

[build-dependencies]
av-app-icon.workspace = true

[lints]
workspace = true
