diff --git a/README.md b/README.md
index 0eec4a1..e81be0e 100644
--- a/README.md
+++ b/README.md
@@ -236,3 +236,25 @@ The DejaVu fonts under `tools/font/vendor/`, and the generated glyph atlas in
 `src/app/font_data.c`, remain covered by the bundled
 [Bitstream Vera/DejaVu license](tools/font/vendor/LICENSE_DEJAVU). That license is separate from
 the project's MIT license and must be retained when redistributing those assets.
+
+
+### macOS host tools
+
+The vendor-bulk application now also opens through IOKit (no kernel extension).
+Install Python `pyserial` and `Pillow`; Xcode command-line tools provide `cc`.
+`python3 tools/host.py test` runs the portable C suites;
+`python3 tools/host.py sim operator` renders the actual operator UI code to PNG.
+The existing `tools/deploy/flash_usb.py build/app.srec` and `screenshot.py`
+commands select IOKit on macOS and WinUSB on Windows. The updater releases the
+av-frameworks daemon handle through maintenance mode, verifies the image CRC,
+and resumes daemon ownership after launch. CDC detachment after `run` is expected.
+
+### Device settings and generic labels (September 2026)
+
+The local settings page groups firmware, serial, build and uptime in two columns,
+with separate calibration, restore and brightness cards. Brightness uses a large
+percentage, a full-width range and explicit Cancel/Save buttons. Local editors
+survive host Reveal, descriptor replacement and disconnect; Back returns to the
+latest application page. Generic cells wrap labels onto two lines so axis suffixes
+remain readable, use brighter units and share the settings palette. Full clears
+use bounded strips to avoid remnants of the previous page.
diff --git a/docs/protocol.md b/docs/protocol.md
index 743d4b3..ea0c304 100644
--- a/docs/protocol.md
+++ b/docs/protocol.md
@@ -304,8 +304,12 @@ the heartbeat's page field is the recovery copy if a queued `SCREEN` is missed.
 It is emitted only when HELLO negotiation selects minor version 1 or newer; EMP/1.0 peers keep
 using the heartbeat page field and are never sent an opcode they did not negotiate.
 
-Physical slot order is top-row-first: slots 0â€“3 are left-to-right above the display and 4â€“7
-left-to-right below it. Sparse holes and absolute IDs retain those positions. Drilled Number
+Physical knob numbering is top-row-first: inputs 0–3 run left-to-right above the display,
+4–7 below it. Generic pages with 1–4 occupied controls place them on bottom knobs 4–7,
+left-to-right in ascending descriptor ID order. This includes short final pages and sparse
+pages; unused top knobs have no field assignment. Pages with 5–8 controls use the normal
+top-first slot positions. Descriptor IDs, page boundaries, SCREEN ranges and edit/focus IDs
+remain unchanged. The opt-in operator layout retains its defined action/settings positions. Drilled Number
 fields use the opposite knob row as a four-place digit window; the outer buttons move toward
 more/less-significant places within the value and precision bounds. Choice and Color disable the
 outer pair. Choice traverses without wrapping; Color uses R/G/B/optional-A component knobs while
@@ -460,3 +464,33 @@ harnesses cannot do.
 - Font atlas coverage for non-Latin UTF-8 — the protocol is clean; rendering is an asset call.
 - Whether the USB-C socket is physically FS-only. Very strong inference from the pin table and
   the descriptor; confirm on the PCB before assuming High Speed is unreachable.
+
+
+## EMP/1.2: actions and operator pages
+
+Negotiation remains `min(host_minor, device_minor)`. EMP/1.0 and 1.1 peers retain
+all existing descriptor, input and grid behaviour.
+
+- Field kind 5 is a momentary **Action**, accepted only for negotiated minor >=2.
+  Turning it, resetting it, holding it or releasing it cannot execute it.
+- Input opcode `0x59` on the input channel is **ACTIVATE**: exactly 14 bytes,
+  little endian `revision:u64, activation_sequence:u32, field_id:u16`.
+  A press edge advances the session sequence. Hosts validate revision, field kind
+  and monotonic wrapping sequence; duplicates and old revisions are discarded.
+- The reserved u16 in DESC_BEGIN is now layout flags. Bit 0 opts into the operator
+  layout for minor >=2. Zero retains the established grid. Hosts must send zero
+  to older peers and omit Action fields when actions are unsupported.
+- Text values are retained in a bounded 96-byte field buffer and repainted on
+  VALUES updates. Long UTF-8 values truncate at a character boundary and emit
+  STRING_TRUNCATED diagnostics.
+
+The operator layout is an explicit slot contract, not automatic schema styling.
+This opt-in layout retains its original descriptor slot contract: slots 0–3 correspond
+to bottom knobs; 4–7 correspond to top knobs. Physical input indices remain top-first;
+the layout alone maps its semantic slots to those inputs. Generic descriptors always
+place slot 0 at the top left. Page 0 uses
+0 axis, 1 jog step, 2 speed, 3 observed position text (optionally followed by a newline and operation status), 4 jog minus, 5 jog plus,
+6 home, 7 stop. Press bottom-right for settings. Page 1 uses 8 route,
+9 acceleration, 10 minimum speed, 11 target, 12 move, 13 connection Boolean,
+14 operation text, 15 stop. Button 1 returns to page 0. Settings turn in place;
+they never borrow the action row for digit editing. Other apps opt out by default.
diff --git a/src/app/touch_cal.c b/src/app/touch_cal.c
index e0ca8ff..22b4f1c 100644
--- a/src/app/touch_cal.c
+++ b/src/app/touch_cal.c
@@ -71,9 +71,12 @@ static void commit_pot(uint32_t now_ms)
     record.magic = TOUCH_CAL_MAGIC;
     record.version = TOUCH_CAL_VERSION;
     record.generation++;
-    record.valid_mask = st.valid_mask;
-    memcpy(record.threshold, st.threshold, sizeof(record.threshold));
-    memcpy(record.gain, st.gain, sizeof(record.gain));
+    /* Flash v1 stays bottom-first so upgrades and downgrades retain physical calibration. */
+    record.valid_mask = ((st.valid_mask << 4) | (st.valid_mask >> 4)) & 0xffu;
+    for (unsigned p = 0; p < 8; p++) {
+        record.threshold[p ^ 4u] = st.threshold[p];
+        record.gain[p ^ 4u] = st.gain[p];
+    }
     record.checksum = persist_touch_cal_checksum(&record);
     if (persist_touch_cal_write(&record) != 0) { fail("Could not save calibration"); return; }
 
@@ -98,12 +101,12 @@ void touch_cal_init(void)
         st.gain[p] = QT2120_DEFAULT_GAIN;
     }
     if (persist_touch_cal_read(&record)) {
-        st.valid_mask = (uint8_t)(record.valid_mask & 0xFFu);
+        st.valid_mask = (uint8_t)((record.valid_mask << 4) | (record.valid_mask >> 4));
         for (unsigned p = 0; p < 8u; p++) {
             if (!(st.valid_mask & (1u << p))) continue;
-            if (qt2120_apply_pot_config((uint8_t)p, record.threshold[p], record.gain[p]) == 0) {
-                st.threshold[p] = record.threshold[p];
-                st.gain[p] = record.gain[p];
+            if (qt2120_apply_pot_config((uint8_t)p, record.threshold[p ^ 4u], record.gain[p ^ 4u]) == 0) {
+                st.threshold[p] = record.threshold[p ^ 4u];
+                st.gain[p] = record.gain[p ^ 4u];
             } else {
                 st.valid_mask &= (uint8_t)~(1u << p);
             }
diff --git a/src/app/ui.c b/src/app/ui.c
index 99b564a..693e6c4 100644
--- a/src/app/ui.c
+++ b/src/app/ui.c
@@ -36,21 +36,21 @@
 
 /* A restrained palette: dark ground, one accent, grey for anything secondary. Colour is reserved
  * for state that matters rather than spent on decoration. */
-#define C_BG        0x0000
-#define C_CELL      0x1082      /* very dark grey: separates a cell from the ground */
+#define C_BG        0x0862
+#define C_CELL      0x18C4      /* very dark grey: separates a cell from the ground */
 #define C_CELL_HOT  0x2124      /* under a finger */
 #define C_CELL_DOWN 0x3186      /* switch held: brighter than touched, because it is deliberate */
 #define C_OUTLINE   0x39E7      /* quiet idle frames and separators */
 #define C_TITLE     0xBDF7      /* headings and field names */
 #define C_ICON      0xCE79      /* enabled button glyphs */
-#define C_NAV       0xA45E      /* current page and navigation selection */
+#define C_NAV       0x4DD6      /* current page and navigation selection */
 #define C_VALUE     0xFFFF
-#define C_UNIT      0x6B4D
-#define C_ACCENT    0x07FF      /* cyan: bars, the drilled cell, the live digit */
+#define C_UNIT      0xBDF7
+#define C_ACCENT    0x4DD6      /* cyan: bars, the drilled cell, the live digit */
 #define C_TRACK     0x18E3      /* the EMPTY part of a bar */
 #define C_DIM       0x4208      /* a digit outside the value's magnitude */
 #define C_DISABLED  0x2965      /* an affordance that would do nothing */
-#define C_TOGGLE_ON 0x07E0
+#define C_TOGGLE_ON 0x4DD6
 #define C_ALERT     0xFBE0      /* amber: faults and failed calibration */
 #define C_LINK      0x4C7F      /* knob-to-digit link lines */
 
@@ -109,6 +109,14 @@
 
 /* ------------------------------------------------------------------ helpers */
 
+/* Short fills finish within the controller's bounded busy wait, including on view changes. */
+static void clear_screen(void)
+{
+    ra8876_set_fg(C_BG);
+    for (int16_t y = 0; y < LCD_HEIGHT; y += 80)
+        ra8876_fill_rect(0, y, LCD_WIDTH, 80);
+}
+
 /* Scale an RGB565 towards black, for the "everything but the digit under your finger fades
  * back" emphasis that makes the drilled view readable at a glance. */
 static uint16_t dim(uint16_t c, uint8_t pct)
@@ -129,7 +137,7 @@ static void frame(int16_t x, int16_t y, int16_t w, int16_t h, int16_t t, uint16_
 }
 
 /* A boolean is a state, not a tiny two-button menu. One strong tile reads cleanly at a glance:
- * green means ON, while OFF stays neutral and relies on its word rather than inverse chrome. */
+ * the accent means ON, while OFF stays neutral and relies on its word rather than inverse chrome. */
 static void draw_bool_tile(int16_t x, int16_t y, int16_t w, int16_t h, int on,
                            const font_t *face)
 {
@@ -197,6 +205,11 @@ static uint16_t color565(const surf_field_t *f)
  * "Mode: 2" instead of "Mode: Additive". */
 static void value_text(const surf_field_t *f, char *out, uint32_t cap)
 {
+    if (f->kind == EMP_KIND_ACTION) { if (cap >= 6u) memcpy(out, "PRESS", 6u); return; }
+    if (f->value_tag == EMP_VAL_TEXT) {
+        uint32_t n = (uint32_t)strlen(f->text); if (n >= cap) n = cap - 1u;
+        memcpy(out, f->text, n); out[n] = 0; return;
+    }
     switch (f->value_tag) {
     case EMP_VAL_BOOL:
         if (cap > 4) memcpy(out, f->boolean ? "ON" : "OFF", f->boolean ? 3u : 4u);
@@ -305,8 +318,7 @@ static void forget_everything(void)
 void ui_reset(void)
 {
     forget_everything();
-    ra8876_set_fg(C_BG);
-    ra8876_fill_rect(0, 0, LCD_WIDTH, LCD_HEIGHT);
+    clear_screen();
 }
 
 /* ------------------------------------------------------------------ page position */
@@ -331,12 +343,12 @@ static void draw_page_strip(int16_t y)
         int16_t map_w = (int16_t)(pages * group_w + (pages - 1u) * page_gap);
         int16_t x0 = (int16_t)((LCD_WIDTH - map_w) / 2);
         int16_t y0 = (int16_t)(y + (32 - (2 * block + gap)) / 2);
-        for (uint16_t i = 0; i < total; i++) {
-            if (!surf_field(i)) continue;
+        for (uint16_t i = 0; i < pages * UI_FIELDS_PER_PAGE; i++) {
             uint16_t pg = (uint16_t)(i / UI_FIELDS_PER_PAGE);
             uint16_t lane = (uint16_t)(i % UI_FIELDS_PER_PAGE);
+            if (ui_state_field_for_page((uint16_t)(pg * UI_FIELDS_PER_PAGE), lane) < 0) continue;
             int16_t col = (int16_t)(lane % UI_ROW_SPLIT);
-            int16_t row = lane < UI_ROW_SPLIT ? 1 : 0;
+            int16_t row = lane < UI_ROW_SPLIT ? 0 : 1;
             int16_t x = (int16_t)(x0 + pg * (group_w + page_gap) + col * (block + gap));
             ra8876_set_fg(pg == page / UI_FIELDS_PER_PAGE ? C_NAV : C_OUTLINE);
             ra8876_fill_rect(x, (int16_t)(y0 + row * (block + gap)), block, block);
@@ -417,7 +429,7 @@ static void draw_legend(int full)
 static void cell_geometry(unsigned knob, int16_t *x, int16_t *y)
 {
     *x = CELL_X(knob % COLS);
-    *y = (knob < UI_ROW_SPLIT) ? ROW_BOT_Y : ROW_TOP_Y;
+    *y = (knob < UI_ROW_SPLIT) ? ROW_TOP_Y : ROW_BOT_Y;
 }
 
 static uint16_t cell_frame_color(int touched, int pressed, int drilled, int active)
@@ -444,8 +456,8 @@ static void draw_cell_dynamic(unsigned knob, const surf_field_t *f)
     const uint16_t bg = C_CELL;
 
     ra8876_set_fg(bg);
-    ra8876_fill_rect((int16_t)(x + 2), (int16_t)(y + 48),
-                     (int16_t)(CELL_W - 4), (int16_t)(h - 50));
+    ra8876_fill_rect((int16_t)(x + 2), (int16_t)(y + 70),
+                     (int16_t)(CELL_W - 4), (int16_t)(h - 72));
 
     if (!f) {
         text_draw_centred(&font_ui, (int16_t)(x + CELL_W / 2),
@@ -460,7 +472,7 @@ static void draw_cell_dynamic(unsigned knob, const surf_field_t *f)
 
     /* The value on the cell's centre line, left-aligned: a column of numbers sharing a left edge
      * is far easier to compare down the panel than one that shifts with every digit count. */
-    int16_t vy = (int16_t)(y + h / 2 - text_height(&font_value) / 2);
+    int16_t vy = (int16_t)(y + 14 + h / 2 - text_height(&font_value) / 2);
     value_text(f, buf, sizeof(buf));
 
     if (f->value_tag == EMP_VAL_BOOL) {
@@ -485,6 +497,7 @@ static void draw_cell_dynamic(unsigned knob, const surf_field_t *f)
     /* Numbers get the monospaced face so digits do not shift as they change; words get the
      * proportional one, since monospacing "Sawtooth" buys nothing and costs the width. */
     const font_t *vf = (f->value_tag == EMP_VAL_NUMBER) ? &font_value : &font_ui;
+    if (text_width(vf, buf) > avail) vf = &font_ui;
     fit_text(vf, buf, avail);
 
     uint16_t vc = C_VALUE;
@@ -500,8 +513,8 @@ static void draw_cell_dynamic(unsigned knob, const surf_field_t *f)
 
     /* A fader down the cell's right edge: it uses the height, four of them side by side read as
      * a set, and it matches the range bar in the drilled view. */
-    int16_t by = (int16_t)(y + 48);
-    int16_t bh = (int16_t)(h - 48 - 2 * SP);
+    int16_t by = (int16_t)(y + 70);
+    int16_t bh = (int16_t)(h - 70 - 2 * SP);
     int16_t ext = bar_extent(f, bh);
 
     ra8876_set_fg(C_TRACK);
@@ -515,6 +528,35 @@ static void draw_cell_dynamic(unsigned knob, const surf_field_t *f)
     }
 }
 
+/* Preserve identifying suffixes (Offset X/Y, frequency units) before truncating a title.
+ * Labels get two lines, independent of the value repaint region below y+70. */
+static void draw_cell_label(int16_t x, int16_t y, char *label)
+{
+    const int16_t width = CELL_W - 4 * SP;
+    const font_t *faces[] = { &font_ui, &font_small };
+    for (unsigned face = 0; face < 2; face++) {
+        const font_t *font = faces[face];
+        if (text_width(font, label) <= width) {
+            text_draw(font, x, y, label, C_TITLE, C_CELL);
+            return;
+        }
+        for (unsigned split = (unsigned)strlen(label); split > 0; split--) {
+            if (label[split] != ' ') continue;
+            label[split] = 0;
+            int fits = text_width(font, label) <= width
+                    && text_width(font, label + split + 1) <= width;
+            if (fits) {
+                text_draw(font, x, y, label, C_TITLE, C_CELL);
+                text_draw(font, x, y + text_height(font), label + split + 1, C_TITLE, C_CELL);
+                return;
+            }
+            label[split] = ' ';
+        }
+    }
+    fit_text(&font_small, label, width);
+    text_draw(&font_small, x, y, label, C_TITLE, C_CELL);
+}
+
 static void draw_cell(unsigned knob, const surf_field_t *f,
                       int touched, int pressed, int drilled, int active)
 {
@@ -527,9 +569,7 @@ static void draw_cell(unsigned knob, const surf_field_t *f,
     if (f) {
         char label[48];
         str_of(label, sizeof(label), f->label_off, f->label_len);
-        fit_text(&font_ui, label, (int16_t)(CELL_W - 4 * SP));
-        text_draw(&font_ui, (int16_t)(x + 2 * SP), (int16_t)(y + SP),
-                  label, C_TITLE, C_CELL);
+        draw_cell_label((int16_t)(x + 2 * SP), (int16_t)(y + SP), label);
     }
     draw_cell_dynamic(knob, f);
 }
@@ -538,6 +578,7 @@ static void draw_cell(unsigned knob, const surf_field_t *f,
  * that still owns ordinary fields while the opposite row edits the focused value. */
 static void render_cells(int full, unsigned first, unsigned last)
 {
+    static char last_text[8][96];
     const ui_state_t *st = ui_state();
 
     for (unsigned k = first; k < last; k++) {
@@ -560,7 +601,7 @@ static void render_cells(int full, unsigned first, unsigned last)
         int16_t bh = (int16_t)(ROW_H - 48 - 2 * SP);
         int value_dirty = static_dirty;
         if (f && !value_dirty) {
-            value_dirty = last_cell[k].boolean != f->boolean
+            value_dirty = strcmp(last_text[k], f->text) != 0 || last_cell[k].boolean != f->boolean
                        || last_cell[k].choice  != f->choice
                        || last_cell[k].number  != f->number
                        || last_cell[k].color_count != f->color_count
@@ -582,6 +623,7 @@ static void render_cells(int full, unsigned first, unsigned last)
         last_cell[k].drilled = drilled;
         last_cell[k].active  = (uint8_t)active;
         last_cell[k].id      = f ? f->id : 0xFFFF;
+        if (f) memcpy(last_text[k], f->text, sizeof(f->text)); else last_text[k][0] = 0;
         last_cell[k].tag     = f ? f->value_tag : 0;
         last_cell[k].number  = f ? f->number : 0;
         last_cell[k].choice  = f ? f->choice : 0;
@@ -593,13 +635,136 @@ static void render_cells(int full, unsigned first, unsigned last)
     }
 }
 
+/* Operator overview: commands at the physical top knobs, large observed readout,
+ * settings at the bottom knobs. Opt-in descriptor flag; other applications keep the grid.
+ * Each region repaints only on change, so telemetry never flashes the whole panel. */
+static void render_operator(int full)
+{
+    static char previous[8][96];
+    static uint16_t previous_press;
+    const ui_state_t *st = ui_state();
+    const uint16_t ground = 0x0862, panel = 0x18c4, ink = 0xffff, muted = 0xbdd7;
+    const uint16_t stop_bg = 0x9808;
+    if (full) {
+        ra8876_set_fg(ground);
+        for (int16_t y = 0; y < 480; y += 80) ra8876_fill_rect(0, y, 800, 80);
+        memset(previous, 0xff, sizeof(previous)); previous_press = 0xffff;
+        text_draw(&font_small, 24, 114, "MANUAL CONTROL", muted, ground);
+        text_draw_right(&font_small, 776, 114, "1 / 2", muted, ground);
+        text_draw(&font_small, 24, 318, "ACTUAL POSITION / rev", muted, ground);
+    }
+    for (unsigned k = 0; k < 8; ++k) {
+        const surf_field_t *f = surf_field((uint16_t)k);
+        char value[96] = {0}; if (f) value_text(f, value, sizeof(value));
+        int pressed = (st->press_mask & (1u << (k ^ 4u))) != 0;
+        if (!full && strcmp(previous[k], value) == 0 &&
+            ((previous_press ^ st->press_mask) & (1u << (k ^ 4u))) == 0) continue;
+        memcpy(previous[k], value, sizeof(value));
+        if (k >= 4) {
+            int16_t x = (int16_t)(16 + (k - 4) * 196);
+            uint16_t bg = k == 7 ? stop_bg : panel;
+            if (pressed) bg = k == 7 ? 0xc108 : 0x3269;
+            ra8876_set_fg(bg); ra8876_fill_rect(x, 12, 184, 88);
+            char label[48] = {0}; if (f) str_of(label, sizeof(label), f->label_off, f->label_len);
+            /* Whole words on two lines beat truncating the action an operator will execute. */
+            if (k == 7) { text_draw_centred(&font_ui, x + 92, 22, "STOP", ink, bg);
+                text_draw_centred(&font_small, x + 92, 60, "PRESS / ESCAPE", ink, bg); }
+            else { fit_text(&font_ui, label, 164);
+                text_draw_centred(&font_ui, x + 92, 22, label, ink, bg);
+                text_draw_centred(&font_small, x + 92, 60, "PRESS", muted, bg); }
+        } else if (k == 3) {
+            char *status = strchr(value, '\n');
+            if (status) {
+                *status++ = 0;
+                ra8876_set_fg(ground); ra8876_fill_rect(24, 114, 660, 30);
+                fit_text(&font_small, status, 660);
+                text_draw(&font_small, 24, 114, status, muted, ground);
+            }
+            ra8876_set_fg(ground); ra8876_fill_rect(24, 157, 752, 154);
+            int numeric = value[0] && (value[0] == '-' || (value[0] >= '0' && value[0] <= '9'));
+            const font_t *face = numeric ? &font_display : &font_ui;
+            if (text_width(face, value) > 740) face = &font_value;
+            fit_text(face, value, 740);
+            text_draw(face, 24, numeric ? 158 : 208, value, ink, ground);
+        } else {
+            int16_t x = (int16_t)(16 + k * 196);
+            ra8876_set_fg(panel); ra8876_fill_rect(x, 366, 184, 102);
+            const char *label = k == 0 ? "AXIS" : k == 1 ? "STEP / rev" : "SPEED / ustep/s";
+            text_draw(&font_small, x + 12, 374, label, muted, panel);
+            const font_t *face = k == 0 ? &font_ui : &font_value;
+            fit_text(face, value, 160);
+            text_draw(face, x + 12, 411, value, ink, panel);
+        }
+    }
+    if (full) {
+        ra8876_set_fg(panel); ra8876_fill_rect(604, 366, 180, 102);
+        text_draw(&font_small, 616, 374, "MORE SETTINGS", muted, panel);
+        text_draw(&font_ui, 616, 414, "PRESS  >", ink, panel);
+    }
+    previous_press = st->press_mask;
+}
+
+/* Settings stay on the same visual scale and never take over the Stop knob. */
+static void render_operator_settings(int full)
+{
+    static char previous[8][96];
+    static uint16_t previous_press;
+    const ui_state_t *st = ui_state();
+    const uint16_t ground = 0x0862, panel = 0x18c4, ink = 0xffff, muted = 0xbdd7;
+    if (full) {
+        ra8876_set_fg(ground);
+        for (int16_t y = 0; y < 480; y += 80) ra8876_fill_rect(0, y, 800, 80);
+        memset(previous, 0xff, sizeof(previous)); previous_press = 0xffff;
+        text_draw(&font_small, 24, 114, "MOTION SETTINGS", muted, ground);
+        text_draw_right(&font_small, 776, 114, "2 / 2", muted, ground);
+        text_draw(&font_small, 24, 318, "BUTTON 1: BACK TO MANUAL CONTROL", muted, ground);
+    }
+    for (unsigned k = 0; k < 8; ++k) {
+        const surf_field_t *f = surf_field((uint16_t)(8 + k));
+        char value[96] = {0}; if (f) value_text(f, value, sizeof(value));
+        if (!full && strcmp(previous[k], value) == 0 && !((previous_press ^ st->press_mask) & (1u << (k ^ 4u)))) continue;
+        memcpy(previous[k], value, sizeof(value));
+        if (k < 4) {
+            int16_t x = (int16_t)(16 + k * 196);
+            const char *labels[] = { "COMMAND VIA", "ACCEL / ustep/s2", "MIN SPEED", "TARGET / rev" };
+            ra8876_set_fg(panel); ra8876_fill_rect(x, 366, 184, 102);
+            text_draw(&font_small, x + 12, 374, labels[k], muted, panel);
+            const font_t *face = k == 0 ? &font_ui : &font_value;
+            fit_text(face, value, 160); text_draw(face, x + 12, 411, value, ink, panel);
+        } else if (k == 4 || k == 7) {
+            int16_t x = k == 4 ? 16 : 604;
+            uint16_t bg = k == 7 ? 0x9808 : panel;
+            if (st->press_mask & (1u << (k ^ 4u))) bg = k == 7 ? 0xc108 : 0x3269;
+            ra8876_set_fg(bg); ra8876_fill_rect(x, 12, 184, 88);
+            text_draw_centred(&font_ui, x + 92, 22, k == 4 ? "Move" : "STOP", ink, bg);
+            text_draw_centred(&font_small, x + 92, 60, k == 4 ? "TO TARGET" : "PRESS / ESCAPE", ink, bg);
+        } else if (k == 5) {
+            ra8876_set_fg(ground); ra8876_fill_rect(212, 12, 380, 88);
+            text_draw(&font_small, 224, 24, "COMMAND ROUTE", muted, ground);
+            int connected = f && ((f->value_tag == EMP_VAL_BOOL && f->boolean) ||
+                (f->value_tag == EMP_VAL_TEXT && strcmp(f->text, "true") == 0));
+            text_draw(&font_ui, 224, 56, connected ? "Connected" : "Disconnected", ink, ground);
+        } else {
+            ra8876_set_fg(ground); ra8876_fill_rect(24, 170, 752, 124);
+            text_draw(&font_small, 24, 170, "OPERATION", muted, ground);
+            fit_text(&font_ui, value, 744); text_draw(&font_ui, 24, 218, value, ink, ground);
+        }
+    }
+    previous_press = st->press_mask;
+}
+
 static void render_overview(int full)
 {
     const ui_state_t *st = ui_state();
+    static int was_operator = -1;
+    int operator_page = surf_operator_layout() && st->page < 16 ? (int)(st->page / 8 + 1) : 0;
+    if (operator_page != was_operator) full = 1;
+    was_operator = operator_page;
+    if (operator_page == 1) { render_operator(full); return; }
+    if (operator_page == 2) { render_operator_settings(full); return; }
 
     if (full) {
-        ra8876_set_fg(C_BG);
-        ra8876_fill_rect(0, 0, LCD_WIDTH, LCD_HEIGHT);
+        clear_screen();
     }
     if (full || st->page != last_page) {
         draw_page_strip(PAGE_Y);
@@ -611,10 +776,34 @@ static void render_overview(int full)
 
 /* ------------------------------------------------------------------ local system views */
 
-static void draw_info_line(int16_t y, const char *label, const char *value, uint16_t colour)
+static void system_fact(int16_t x, int16_t y, const char *label, const char *value, const font_t *face)
 {
-    text_draw(&font_small, MARGIN_X, y, label, C_TITLE, C_BG);
-    text_draw(&font_ui, 250, (int16_t)(y - 5), value, colour, C_BG);
+    text_draw(&font_small, x, y, label, C_TITLE, C_BG);
+    char copy[80];
+    strncpy(copy, value, sizeof(copy) - 1); copy[sizeof(copy) - 1] = 0;
+    fit_text(face, copy, 352);
+    text_draw(face, x, (int16_t)(y + 28), copy, C_VALUE, C_BG);
+}
+
+static void draw_system_footer(int full)
+{
+    const ui_state_t *s = ui_state();
+    static uint16_t old_buttons = 0xffff;
+    static int old_mode = -1;
+    if (!full && old_buttons == s->btn_mask && old_mode == (int)s->mode) return;
+    old_buttons = s->btn_mask; old_mode = s->mode;
+    const char *back = s->mode == UI_MODE_BRIGHTNESS ? "1  Cancel" : "1  Back";
+    const char *next = s->mode == UI_MODE_SYSTEM ? "5  Restart"
+        : s->mode == UI_MODE_BRIGHTNESS ? "5  Save brightness"
+        : s->mode == UI_MODE_CAL_SELECT ? "5  Calibrate all"
+        : s->mode == UI_MODE_RESTORE_CONFIRM ? "5  Restore defaults"
+        : s->mode == UI_MODE_CAL_RUN ? (s->cal_phase == 7 ? "5  Retry" : "5  Done") : "5  Release to restart";
+    ra8876_set_fg(C_BG); ra8876_fill_rect(0, 418, LCD_WIDTH, 62);
+    ra8876_set_fg(C_OUTLINE); ra8876_fill_rect(24, 418, 752, 1);
+    if (ui_state_button_enabled(UI_BTN_BACK))
+        text_draw(&font_small, 24, 442, back, s->btn_mask & 1 ? C_ACCENT : C_TITLE, C_BG);
+    if (ui_state_button_enabled(UI_BTN_SYSTEM))
+        text_draw_right(&font_small, 776, 442, next, s->btn_mask & (1u << UI_BTN_SYSTEM) ? C_ACCENT : C_TITLE, C_BG);
 }
 
 /* The drawing order deliberately mirrors the enclosure: dials 5-8 above the screen and 1-4
@@ -625,7 +814,7 @@ static void draw_cal_dial_map(int active, int selection)
     const ui_state_t *s = ui_state();
     for (unsigned p = 0; p < 8u; p++) {
         int16_t x = CELL_X(p % UI_ROW_SPLIT);
-        int16_t y = p < UI_ROW_SPLIT ? (selection ? 302 : 338) : (selection ? 116 : 72);
+        int16_t y = p < UI_ROW_SPLIT ? (selection ? 116 : 72) : (selection ? 302 : 338);
         int16_t h = selection ? 92 : 58;
         uint16_t bg = active == (int)p ? C_CELL_DOWN
                     : ((s->cal_valid_mask & (1u << p)) ? C_CELL_HOT : C_CELL);
@@ -662,54 +851,33 @@ static void render_brightness_editor(void)
     const ui_state_t *s = ui_state();
     char value[16];
     (void)text_format(value, sizeof(value), (double)s->brightness_preview, 0);
-    unsigned n = 0;
-    while (value[n]) n++;
-    if (n + 1u < sizeof(value)) { value[n++] = '%'; value[n] = 0; }
-
-    text_draw_centred(&font_display, LCD_WIDTH / 2, 92, value, C_VALUE, C_BG);
-    text_draw_centred(&font_ui, LCD_WIDTH / 2, 178,
-                      "Turn any dial to adjust", C_TITLE, C_BG);
-
-    const int16_t bx = 100, by = 330, bw = 600, bh = 34;
-    ra8876_set_fg(C_TRACK);
-    ra8876_fill_rect(bx, by, bw, bh);
+    unsigned n = (unsigned)strlen(value); value[n++] = '%'; value[n] = 0;
+    text_draw_centred(&font_display, 400, 94, value, C_VALUE, C_BG);
+    text_draw_centred(&font_small, 400, 246, "Turn any dial to adjust", C_TITLE, C_BG);
+    const int16_t bx = 96, by = 306, bw = 608;
+    ra8876_set_fg(C_TRACK); ra8876_fill_rect(bx, by, bw, 12);
     int16_t fill = (int16_t)(((uint32_t)(s->brightness_preview - 10u) * bw) / 90u);
-    if (fill > 0) {
-        ra8876_set_fg(C_ACCENT);
-        ra8876_fill_rect(bx, by, fill, bh);
-    }
-    frame(bx, by, bw, bh, 2, C_OUTLINE);
-
-    for (unsigned i = 0; i <= 9u; i++) {
-        int16_t x = (int16_t)(bx + (int32_t)bw * i / 9);
-        ra8876_set_fg(C_OUTLINE);
-        ra8876_fill_rect((int16_t)(x - 1), (int16_t)(by + bh + 8), 2,
-                         i == 0u || i == 9u ? 12 : 7);
-    }
-    text_draw(&font_small, bx, (int16_t)(by + bh + 28), "10%", C_TITLE, C_BG);
-    text_draw(&font_small, (int16_t)(bx + bw - text_width(&font_small, "100%")),
-              (int16_t)(by + bh + 28), "100%", C_TITLE, C_BG);
-
+    if (fill > 0) { ra8876_set_fg(C_ACCENT); ra8876_fill_rect(bx, by, fill, 12); }
+    text_draw(&font_small, bx, 332, "10%", C_TITLE, C_BG);
+    text_draw_right(&font_small, bx + bw, 332, "100%", C_TITLE, C_BG);
     if (s->brightness_save_error)
-        text_draw_centred(&font_small, LCD_WIDTH / 2, 418,
-                          "Could not save brightness. Try Done again.", C_ALERT, C_BG);
+        text_draw_centred(&font_small, 400, 378, "Could not save. Press button 5 to retry.", C_ALERT, C_BG);
 }
 
 static void render_system_view(int full)
 {
     const ui_state_t *s = ui_state();
     if (full) {
-        ra8876_set_fg(C_BG);
-        ra8876_fill_rect(0, 0, LCD_WIDTH, LCD_HEIGHT);
+        clear_screen();
     }
 
-    const char *title = "SYSTEM / SETTINGS";
+    const char *title = "Device settings";
     if (s->mode == UI_MODE_REBOOT_WAIT) title = "RESTARTING";
     else if (s->mode == UI_MODE_CAL_SELECT) title = "TOUCH CALIBRATION";
     else if (s->mode == UI_MODE_CAL_RUN) title = "CALIBRATING TOUCH";
     else if (s->mode == UI_MODE_RESTORE_CONFIRM) title = "RESTORE TOUCH DEFAULTS";
-    else if (s->mode == UI_MODE_BRIGHTNESS) title = "BRIGHTNESS";
-    text_draw(&font_ui, MARGIN_X, 18, title, C_TITLE, C_BG);
+    else if (s->mode == UI_MODE_BRIGHTNESS) title = "Brightness";
+    text_draw(&font_ui, 24, 20, title, C_VALUE, C_BG);
     ra8876_set_fg(C_OUTLINE);
     ra8876_fill_rect(MARGIN_X, 58, LCD_WIDTH - 2 * MARGIN_X, 2);
     if (s->mode == UI_MODE_REBOOT_WAIT) {
@@ -735,50 +903,35 @@ static void render_system_view(int full)
         version[k++] = (char)('0' + EMP_VERSION_MAJOR); version[k++] = '.';
         version[k++] = (char)('0' + EMP_VERSION_MINOR); version[k] = 0;
 
-        draw_info_line(76,  "MODEL", emp_device_model(), C_VALUE);
-        draw_info_line(108, "FIRMWARE", version, C_VALUE);
-        draw_info_line(140, "BUILD", emp_device_build_id(), C_VALUE);
-        draw_info_line(172, "SERIAL", emp_device_serial(), C_VALUE);
-        (void)text_format(n, sizeof(n), (double)(s->uptime_ms / 1000u), 0);
-        draw_info_line(204, "UPTIME (S)", n, C_VALUE);
-        draw_info_line(310, "TOUCH / CAL", s->touch_status == 0
-                       ? (s->cal_custom ? "READY / CUSTOM" : "READY / DEFAULT")
-                       : "FAULT", s->touch_status == 0 ? C_TOGGLE_ON : C_ALERT);
-
-        const char *items[3] = {
-            "Touch calibration", "Restore touch defaults", "Brightness"
-        };
-        const int16_t card_gap = SP;
-        const int16_t card_w = (int16_t)((LCD_WIDTH - 2 * MARGIN_X - 2 * card_gap) / 3);
+        text_draw_right(&font_small, 776, 27, emp_device_model(), C_TITLE, C_BG);
+        system_fact(24, 84, "FIRMWARE", version, &font_ui);
+        system_fact(424, 84, "SERIAL NUMBER", emp_device_serial(), &font_ui);
+        system_fact(24, 166, "BUILD", emp_device_build_id(), &font_small);
+        (void)text_format(n, sizeof(n), (double)(s->uptime_ms / 3600000u), 0);
+        unsigned used = (unsigned)strlen(n); n[used++] = 'h'; n[used++] = ' ';
+        (void)text_format(n + used, sizeof(n) - used, (double)((s->uptime_ms / 60000u) % 60u), 0);
+        used = (unsigned)strlen(n); n[used++] = 'm'; n[used] = 0;
+        system_fact(424, 166, "UPTIME", n, &font_small);
+        text_draw(&font_small, 24, 238, "Touch sensors", C_TITLE, C_BG);
+        text_draw(&font_small, 208, 238, s->touch_status == 0
+            ? (s->cal_custom ? "Ready / custom calibration" : "Ready / factory defaults") : "Sensor fault",
+            s->touch_status == 0 ? C_ACCENT : C_ALERT, C_BG);
+        text_draw(&font_small, 24, 276, "Turn any dial to choose / press to open", C_TITLE, C_BG);
+        const char *items[3] = { "Calibrate", "Restore", "Brightness" };
+        const char *details[3] = { "Touch sensors", "Touch defaults", "" };
         for (unsigned i = 0; i < 3; i++) {
-            int16_t x = (int16_t)(MARGIN_X + i * (card_w + card_gap));
-            uint16_t bg = s->system_selection == i ? C_CELL_HOT : C_CELL;
-            ra8876_set_fg(bg);
-            ra8876_fill_rect(x, 342, card_w, 70);
-            frame(x, 342, card_w, 70,
-                  s->system_selection == i ? 3 : 2,
-                  s->system_selection == i ? C_NAV : C_OUTLINE);
-            char label[32];
-            unsigned j = 0;
-            while (items[i][j] && j + 1u < sizeof(label)) {
-                label[j] = items[i][j]; j++;
-            }
-            label[j] = 0;
-            fit_text(&font_small, label, (int16_t)(card_w - 2 * SP));
-            text_draw(&font_small, (int16_t)(x + SP), 352, label, C_TITLE, bg);
-            if (i == 2u) {
-                char pct[12];
-                (void)text_format(pct, sizeof(pct), (double)s->brightness_saved, 0);
-                unsigned p = 0; while (pct[p]) p++;
-                if (p + 1u < sizeof(pct)) { pct[p++] = '%'; pct[p] = 0; }
-                text_draw(&font_small, (int16_t)(x + SP), 382, pct, C_VALUE, bg);
-                int16_t bar_x = (int16_t)(x + 58), bar_y = 388;
-                int16_t bar_w = (int16_t)(card_w - 58 - SP), bar_h = 8;
-                ra8876_set_fg(C_TRACK); ra8876_fill_rect(bar_x, bar_y, bar_w, bar_h);
-                ra8876_set_fg(C_ACCENT);
-                ra8876_fill_rect(bar_x, bar_y,
-                    (int16_t)((uint32_t)bar_w * s->brightness_saved / 100u), bar_h);
+            int16_t x = (int16_t)(24 + i * 256);
+            int selected = s->system_selection == i;
+            uint16_t bg = selected ? C_CELL_HOT : C_CELL;
+            ra8876_set_fg(bg); ra8876_fill_rect(x, 310, 240, 92);
+            if (selected) { ra8876_set_fg(C_ACCENT); ra8876_fill_rect(x, 310, 3, 92); }
+            text_draw(&font_ui, x + 16, 322, items[i], C_VALUE, bg);
+            char pct[12];
+            if (i == 2) {
+                (void)text_format(pct, sizeof(pct), s->brightness_saved, 0);
+                unsigned len = (unsigned)strlen(pct); pct[len++] = '%'; pct[len] = 0;
             }
+            text_draw(&font_small, x + 16, 366, i == 2 ? pct : details[i], C_TITLE, bg);
         }
     } else if (s->mode == UI_MODE_BRIGHTNESS) {
         render_brightness_editor();
@@ -791,7 +944,7 @@ static void render_system_view(int full)
         if (s->cal_phase == 6u) {
             text_draw_centred(&font_value, LCD_WIDTH / 2, 166,
                               "CALIBRATION COMPLETE", C_TOGGLE_ON, C_BG);
-            text_draw_centred(&font_ui, LCD_WIDTH / 2, 310,
+            text_draw_centred(&font_ui, LCD_WIDTH / 2, 264,
                               "Calibration saved", C_VALUE, C_BG);
         } else {
             char dial[16] = { 'D','I','A','L',' ',(char)('1' + s->cal_pot),0 };
@@ -822,7 +975,7 @@ static void render_system_view(int full)
             /* The two-line alert is the widest calibration status. Restore the surrounding
              * chrome afterwards so its background can never leave holes at the card edges. */
             draw_cal_dial_map((int)s->cal_pot, 0);
-            text_draw(&font_ui, MARGIN_X, 18, title, C_TITLE, C_BG);
+            text_draw(&font_ui, 24, 20, title, C_VALUE, C_BG);
             ra8876_set_fg(C_OUTLINE);
             ra8876_fill_rect(MARGIN_X, 58, LCD_WIDTH - 2 * MARGIN_X, 2);
         }
@@ -832,7 +985,7 @@ static void render_system_view(int full)
         text_draw_centred(&font_small, LCD_WIDTH / 2, 225,
                           "Button 1 cancels. Button 5 confirms.", C_TITLE, C_BG);
     }
-    draw_legend(full);
+    draw_system_footer(full);
 }
 
 /* ------------------------------------------------------------------ drilled readout */
@@ -1172,10 +1325,8 @@ static void render_drilled(int full, const surf_field_t *f)
 
 /* ------------------------------------------------------------------ power-on */
 
-/* Panel order, not knob order. cell_geometry puts knobs 0-3 on the BOTTOM row and 4-7 on the
- * top, so filling left-to-right down the panel is 4,5,6,7 then 0,1,2,3. Getting this backwards
- * is invisible in code and obvious on the device, which is why it is a named table. */
-static const uint8_t boot_order[UI_BOOT_CELL_FRAMES] = { 4, 5, 6, 7, 0, 1, 2, 3 };
+/* Boot fills in canonical reading order, the same order used by fields and inputs. */
+static const uint8_t boot_order[UI_BOOT_CELL_FRAMES] = { 0, 1, 2, 3, 4, 5, 6, 7 };
 
 /* The accent rule lives at the bottom of the band -- the one region no knob points at -- so it
  * reads as the surface as a whole coming up rather than as anything about a control. */
@@ -1204,8 +1355,7 @@ static const uint8_t boot_order[UI_BOOT_CELL_FRAMES] = { 4, 5, 6, 7, 0, 1, 2, 3
 void ui_boot_ready_frame(unsigned step)
 {
     if (step == 0) {
-        ra8876_set_fg(C_BG);
-        ra8876_fill_rect(0, 0, LCD_WIDTH, LCD_HEIGHT);
+        clear_screen();
         return;
     }
 
@@ -1253,7 +1403,9 @@ void ui_render(void)
     const ui_state_t *st = ui_state();
     int view = st->mode == UI_MODE_SURFACE ? ((st->focused >= 0) ? 1 : 0)
                                            : (2 + (int)st->mode);
-    int full = (view != last_view);
+    static uint64_t previous_revision = UINT64_MAX;
+    int full = (view != last_view) || previous_revision != surf_applied_revision();
+    previous_revision = surf_applied_revision();
 
     if (st->mode != UI_MODE_SURFACE) {
         static uint8_t old_phase = 0xFFu, old_pot = 0xFFu, old_cycle = 0xFFu;
@@ -1267,7 +1419,7 @@ void ui_render(void)
                     || old_clear_ds != clear_ds
                     || old_brightness != st->brightness_preview
                     || old_save_error != st->brightness_save_error;
-        if (changed) render_system_view(1); else draw_legend(0);
+        if (changed) render_system_view(1); else draw_system_footer(0);
         old_phase = st->cal_phase; old_pot = st->cal_pot; old_cycle = st->cal_cycle;
         old_sel = st->system_selection; old_valid = st->cal_valid_mask; old_custom = st->cal_custom;
         old_brightness = st->brightness_preview;
@@ -1365,9 +1517,9 @@ void ui_render_demo_focused(void)
     surf_demo_descriptor();
     ui_state_init();
     ui_reset();
-    ui_state_push(0, 1, 1000);       /* press the first bottom-row knob: drill in */
-    ui_state_push(0, 0, 1010);
-    ui_state_touch(0x20, 1020);      /* a finger resting on a top-row digit knob */
+    ui_state_push(UI_ROW_SPLIT, 1, 1000);       /* press the first bottom-row knob: drill in */
+    ui_state_push(UI_ROW_SPLIT, 0, 1010);
+    ui_state_touch(0x02, 1020);      /* a finger resting on a top-row digit knob */
     ui_render();
 }
 
@@ -1380,9 +1532,9 @@ void ui_render_demo_focused_top(void)
     surf_demo_descriptor();
     ui_state_init();
     ui_reset();
-    ui_state_push(UI_ROW_SPLIT, 1, 1000);      /* press the first top-row knob */
-    ui_state_push(UI_ROW_SPLIT, 0, 1010);
-    ui_state_touch(0x02, 1020);                /* a finger on bottom knob 1, now a digit knob */
+    ui_state_push(0, 1, 1000);      /* press the first top-row knob */
+    ui_state_push(0, 0, 1010);
+    ui_state_touch(0x20, 1020);                /* a finger on bottom knob 1, now a digit knob */
     ui_render();
 }
 
@@ -1392,6 +1544,7 @@ static void demo_focus_on_page(uint8_t page, uint8_t knob)
     ui_state_init();
     ui_reset();
     if (page) ui_state_button(UI_BTN_PAGE_NEXT, 1, 1000);
+    knob = (uint8_t)ui_state_knob_for((uint16_t)(page * UI_FIELDS_PER_PAGE + knob));
     ui_state_push(knob, 1, 1010);
     ui_state_push(knob, 0, 1020);
     ui_render();
@@ -1410,8 +1563,8 @@ void ui_render_demo_choice_waveform(uint16_t selected)
     ui_state_init();
     ui_reset();
     ui_state_button(UI_BTN_PAGE_NEXT, 1, 1000);
-    ui_state_push(0, 1, 1010);
-    ui_state_push(0, 0, 1020);
+    ui_state_push((unsigned)ui_state_knob_for(8u), 1, 1010);
+    ui_state_push((unsigned)ui_state_knob_for(8u), 0, 1020);
     ui_render();
 }
 
@@ -1421,15 +1574,16 @@ static void demo_choice_labels(const char *const *labels, uint16_t count,
     surf_demo_choice_descriptor(slot, "Choice Width", labels, count, selected);
     ui_state_init();
     ui_reset();
-    ui_state_push(slot, 1, 1000);
-    ui_state_push(slot, 0, 1010);
+    unsigned knob = (unsigned)ui_state_knob_for(slot);
+    ui_state_push(knob, 1, 1000);
+    ui_state_push(knob, 0, 1010);
     ui_render();
 }
 
 void ui_render_demo_choice_short(void)
 {
     static const char *const labels[] = { "A", "B", "C", "D", "E", "F", "G", "H", "I" };
-    demo_choice_labels(labels, 9u, 4u, 0u);
+    demo_choice_labels(labels, 9u, 4u, UI_ROW_SPLIT);
 }
 
 void ui_render_demo_choice_medium(void)
@@ -1437,7 +1591,7 @@ void ui_render_demo_choice_medium(void)
     static const char *const labels[] = {
         "Alpha", "Bravo", "Charlie", "Delta", "Echo", "Foxtrot", "Golf"
     };
-    demo_choice_labels(labels, 7u, 3u, 0u);
+    demo_choice_labels(labels, 7u, 3u, UI_ROW_SPLIT);
 }
 
 void ui_render_demo_choice_long_top(void)
@@ -1445,7 +1599,7 @@ void ui_render_demo_choice_long_top(void)
     static const char *const labels[] = {
         "Resonant Low Pass", "Pulse Width Mod", "Envelope Follower", "Stereo Crossfeed", "Granular Position"
     };
-    demo_choice_labels(labels, 5u, 2u, UI_ROW_SPLIT);
+    demo_choice_labels(labels, 5u, 2u, 0u);
 }
 
 void ui_render_demo_page2(void)
diff --git a/src/app/ui_state.c b/src/app/ui_state.c
index ad6c403..c70e9aa 100644
--- a/src/app/ui_state.c
+++ b/src/app/ui_state.c
@@ -106,7 +106,7 @@ static void enter_focus(int32_t idx)
     /* The digit editor is the OTHER row. See ui_state.h: this is what makes drilling symmetric,
      * so a knob on either row behaves the same way and the readout always appears beside the
      * knobs that edit it rather than always at the top. */
-    st.digit_top = (uint8_t)(((uint32_t)idx - st.page) < UI_ROW_SPLIT);
+    st.digit_top = (uint8_t)(ui_state_knob_for((uint16_t)idx) >= (int32_t)UI_ROW_SPLIT);
     memset(digit_accum, 0, sizeof(digit_accum));
     rebuild_window();
     surf_send_focus(idx, st.touch_mask);
@@ -131,14 +131,43 @@ void ui_state_init(void)
 
 const ui_state_t *ui_state(void) { return &st; }
 
-unsigned ui_digit_row_first(void) { return st.digit_top ? UI_ROW_SPLIT : 0u; }
-unsigned ui_cell_row_first(void)  { return st.digit_top ? 0u : UI_ROW_SPLIT; }
+unsigned ui_digit_row_first(void) { return st.digit_top ? 0u : UI_ROW_SPLIT; }
+unsigned ui_cell_row_first(void)  { return st.digit_top ? UI_ROW_SPLIT : 0u; }
+
+int32_t ui_state_field_for_page(uint16_t first, unsigned knob)
+{
+    if (knob >= UI_FIELDS_PER_PAGE || first >= SURF_MAX_FIELDS) return -1;
+    /* The operator layout keeps its published action/settings positions. Generic pages with
+     * one to four fields use the nearer row in descriptor order. IDs and page boundaries do
+     * not change; even sparse fields are packed only within their existing page. */
+    if (!surf_operator_layout()) {
+        unsigned count = 0;
+        for (unsigned offset = 0; offset < UI_FIELDS_PER_PAGE; offset++)
+            if (surf_field((uint16_t)(first + offset))) count++;
+        if (count <= UI_ROW_SPLIT) {
+            if (knob < UI_ROW_SPLIT) return -1;
+            unsigned wanted = knob - UI_ROW_SPLIT;
+            for (unsigned offset = 0; offset < UI_FIELDS_PER_PAGE; offset++) {
+                if (!surf_field((uint16_t)(first + offset))) continue;
+                if (wanted-- == 0) return (int32_t)(first + offset);
+            }
+            return -1;
+        }
+    }
+    uint16_t slot = (uint16_t)(first + (knob ^ (surf_operator_layout() ? 4u : 0u)));
+    return surf_field(slot) ? (int32_t)slot : -1;
+}
 
 int32_t ui_state_field_for(unsigned knob)
 {
-    if (knob >= UI_FIELDS_PER_PAGE) return -1;
-    uint32_t idx = (uint32_t)st.page + knob;
-    return (idx < SURF_MAX_FIELDS && surf_field((uint16_t)idx)) ? (int32_t)idx : -1;
+    return ui_state_field_for_page(st.page, knob);
+}
+
+int32_t ui_state_knob_for(uint16_t slot)
+{
+    for (unsigned knob = 0; knob < UI_FIELDS_PER_PAGE; knob++)
+        if (ui_state_field_for(knob) == (int32_t)slot) return (int32_t)knob;
+    return -1;
 }
 
 static uint16_t last_page_start(void)
@@ -174,6 +203,11 @@ void ui_state_surface_changed(void)
 
 void ui_state_surface_cleared(void)
 {
+    /* App disconnects must not abandon a local brightness preview or calibration. */
+    if (st.mode != UI_MODE_SURFACE) {
+        ui_state_surface_changed();
+        return;
+    }
     ui_state_init();
     surf_set_page(0);
 }
@@ -182,7 +216,8 @@ int ui_state_reveal(uint16_t slot)
 {
     if (!surf_field(slot)) return 0;
     leave_focus();
-    st.mode = UI_MODE_SURFACE;
+    /* Queue the host page underneath local settings; Back returns to the latest app.
+     * Browser hover is navigation, not permission to close a device editor. */
     uint16_t target = (uint16_t)((slot / UI_FIELDS_PER_PAGE) * UI_FIELDS_PER_PAGE);
     if (st.page == target) surf_set_page(target);
     else set_page(target);
@@ -226,10 +261,11 @@ void ui_state_push(unsigned pot, int pressed, uint32_t now_ms)
     if (pot >= UI_FIELDS_PER_PAGE) return;
 
     uint16_t bit = (uint16_t)(1u << pot);
+    int was_pressed = (st.press_mask & bit) != 0;
     if (pressed) st.press_mask |= bit;
     else         st.press_mask = (uint16_t)(st.press_mask & ~bit);
 
-    if (!pressed) return;               /* act on the press edge; release is only an affordance */
+    if (!pressed || was_pressed) return;               /* act on the press edge; release is only an affordance */
 
     if (st.mode == UI_MODE_SYSTEM) {
         if (st.system_selection == 0u) st.mode = UI_MODE_CAL_SELECT;
@@ -249,6 +285,14 @@ void ui_state_push(unsigned pot, int pressed, uint32_t now_ms)
     }
     if (st.mode != UI_MODE_SURFACE) return;
 
+    if (surf_operator_layout()) {
+        if (st.page == 0 && pot == 7u) { set_page(8); return; }
+        int32_t slot = ui_state_field_for(pot);
+        const surf_field_t *field = slot >= 0 ? surf_field((uint16_t)slot) : 0;
+        if (field && field->kind == EMP_KIND_ACTION) surf_activate((uint16_t)slot);
+        /* Operator settings turn in place; never borrow the action row for digits. */
+        return;
+    }
     if (st.focused >= 0) {
         /* While drilled, the digit row's knobs are digit controls, so pressing one means "done
          * here". The other row still addresses its own fields, so pressing one of those moves
@@ -263,7 +307,10 @@ void ui_state_push(unsigned pot, int pressed, uint32_t now_ms)
         return;
     }
 
-    enter_focus(ui_state_field_for(pot));
+    int32_t slot = ui_state_field_for(pot);
+    const surf_field_t *field = slot >= 0 ? surf_field((uint16_t)slot) : 0;
+    if (field && field->kind == EMP_KIND_ACTION) { surf_activate((uint16_t)slot); return; }
+    enter_focus(slot);
 }
 
 /* ------------------------------------------------------------------ buttons */
@@ -458,12 +505,12 @@ static double clamp_to_bounds(const surf_field_t *f, double v)
     return v;
 }
 
-/* A top knob edits one decimal place of the focused value. Only while drilled: in the overview
+/* An opposite-row knob edits one decimal place of the focused value. Only while drilled: in the overview
  * that same knob is an ordinary field control. */
 static int rotate_digit(unsigned pot, int32_t detents, uint32_t now_ms)
 {
     const surf_field_t *f = surf_field((uint16_t)st.focused);
-    if (!f || f->kind == EMP_KIND_READONLY) return 0;
+    if (!f || (f->kind == EMP_KIND_READONLY || f->kind == EMP_KIND_ACTION)) return 0;
     if (f->kind == EMP_KIND_COLOR) {
         unsigned k = pot - ui_digit_row_first();
         if (k >= f->color_count) return 0;
@@ -538,7 +585,7 @@ int ui_state_rotate(unsigned pot, int32_t detents, uint32_t now_ms)
     if (idx < 0) return 0;
 
     const surf_field_t *f = surf_field((uint16_t)idx);
-    if (!f || f->kind == EMP_KIND_READONLY) return 0;
+    if (!f || (f->kind == EMP_KIND_READONLY || f->kind == EMP_KIND_ACTION)) return 0;
 
     if (f->kind == EMP_KIND_COLOR && f->color_count >= 3u) {
         float color[4];
diff --git a/src/app/ui_state.h b/src/app/ui_state.h
index dfa051a..83f20ae 100644
--- a/src/app/ui_state.h
+++ b/src/app/ui_state.h
@@ -33,12 +33,12 @@
 /* Two numbers that were one, and had to be separated before the page could grow.
  *
  * UI_FIELDS_PER_PAGE used to mean both "how many fields a page holds" and "the knob index where
- * the bottom row ends and the top row begins". Those were equal only by coincidence of a 4+4
+ * the top row ends and the bottom row begins". Those were equal only by coincidence of a 4+4
  * layout. Setting the page size to 8 with them still merged makes `pot >= UI_FIELDS_PER_PAGE`
- * never true, so every knob routes to the bottom-row branch and the entire digit window becomes
+ * never true, so every knob routes to the ordinary-field branch and the entire digit window becomes
  * unreachable code that still compiles. */
 #define UI_FIELDS_PER_PAGE 8u        /* fields visible at once: one per knob */
-#define UI_ROW_SPLIT       4u        /* measured panel order: 0-3 bottom, 4-7 top */
+#define UI_ROW_SPLIT       4u        /* measured panel order: 0-3 top, 4-7 bottom */
 #define UI_DIGITS          4u        /* knobs in whichever row is the drilled editor */
 
 #define UI_DISCRETE_STEP   5         /* detents per step on a Toggle or Choice */
@@ -129,7 +129,7 @@ typedef struct {
 void ui_state_init(void);
 const ui_state_t *ui_state(void);
 
-/* Physical input. `pot` is in measured panel order: 0-3 bottom row, 4-7 top row. */
+/* Physical input. `pot` is in measured panel order: 0-3 top row, 4-7 bottom row. */
 void ui_state_touch(uint16_t touch_mask, uint32_t now_ms);
 void ui_state_push(unsigned pot, int pressed, uint32_t now_ms);
 void ui_state_button(unsigned button, int pressed, uint32_t now_ms);
@@ -149,8 +149,11 @@ int ui_state_reveal(uint16_t slot);
 /* Called every service pass; runs the history settle timer. */
 void ui_state_tick(uint32_t now_ms);
 
-/* Which absolute field a knob addresses, or -1 past the end of a short last page. */
+/* Logical descriptor IDs stay stable; these are the authoritative layout mappings.
+ * Generic pages with 1..4 occupied controls use bottom knobs 4..7, in ID order. */
 int32_t ui_state_field_for(unsigned knob);
+int32_t ui_state_field_for_page(uint16_t first, unsigned knob);
+int32_t ui_state_knob_for(uint16_t slot);
 
 /* First knob index of the row currently acting as the digit editor, and of the row still
  * showing cells. Both are UI_DIGITS wide. Meaningless unless something is drilled. */
diff --git a/src/hal/inputs.c b/src/hal/inputs.c
index 6efcac1..7c000e5 100644
--- a/src/hal/inputs.c
+++ b/src/hal/inputs.c
@@ -189,7 +189,7 @@ uint16_t inputs_adc_raw(unsigned unit, unsigned ch)
 
 /* Rotation channel order, measured on the panel. See inputs.h for why this differs from the
  * switch mapping. */
-const unsigned char inputs_mux_to_pot_rotation[8] = { 4, 0, 1, 5, 2, 7, 3, 6 };
+const unsigned char inputs_mux_to_pot_rotation[8] = { 0, 4, 5, 1, 6, 3, 7, 2 };
 
 /* Front-panel buttons 1..3 are wired in reverse; 4..6 are in order. */
 const unsigned char inputs_button_map[6] = { 2, 1, 0, 3, 4, 5 };
@@ -314,7 +314,7 @@ void inputs_sample(input_sample_t *out)
         inputs_mux_select(ch);
 
         /* Three different mappings off the same channel, all measured on the panel:
-         * rotation is scrambled, the push switch is a row swap, and the buttons have their
+         * rotation is scrambled, the push switch uses channel identity, and the buttons have their
          * first three reversed. Applying each here means nothing above the HAL ever sees the
          * multiplexer's ordering. */
         inputs_read_phases(&out->phase_a[inputs_mux_to_pot_rotation[ch]],
@@ -328,7 +328,7 @@ void inputs_sample(input_sample_t *out)
 }
 
 /* Rotation: panel pot -> multiplexer channel. The inverse of inputs_mux_to_pot_rotation. */
-static const unsigned char pot_to_mux_rotation[8] = { 1, 2, 4, 6, 0, 3, 7, 5 };
+static const unsigned char pot_to_mux_rotation[8] = { 0, 3, 7, 5, 1, 2, 4, 6 };
 
 /* Digital-only channel select. The strobe gates the ANALOG path, so the button and switch
  * senses need no settling at all — measured identical with the strobe either way. Skipping the
diff --git a/src/hal/inputs.h b/src/hal/inputs.h
index 2459dc6..6078fe4 100644
--- a/src/hal/inputs.h
+++ b/src/hal/inputs.h
@@ -14,25 +14,13 @@
 #define INPUT_POTS     8
 #define INPUT_BUTTONS  6
 
-/* Panel pot index (0 = bottom-left) <-> multiplexer channel.
- *
- * There are TWO mappings, and they are genuinely different — which is the single most
- * counter-intuitive thing about this input tree.
- *
- *   - Capacitive touch and the pot push switches use a plain row swap, `(n + 4) mod 8`.
- *   - Pot ROTATION is scrambled: `{4,0,1,5,2,7,3,6}` indexed by multiplexer channel.
- *
- * Both are measured on the panel, not inferred. The rotation table is exactly the stock
- * firmware's `{0,4,5,1,6,3,7,2}` with the same +4 row swap applied, so both paths use the
- * UI's bottom-row-first physical order.
- *
- * An earlier pass here concluded the opposite — that one mapping served both — from noticing
- * that pushes and phase movements appeared on the same channel in an interleaved capture. That
- * was a coincidence of timing, not evidence. Turning each knob and watching which cell moved
- * settled it properly.
+/* Canonical panel order: 0..3 top left-to-right, 4..7 bottom left-to-right.
+ * Push switches use mux channel identity. Rotation has separate wiring, recovered from
+ * stock firmware and checked on the panel: mux -> pot {0,4,5,1,6,3,7,2}.
+ * Do not infer the rotation mapping from push switches: they share a mux but not wiring.
  */
-static inline unsigned inputs_pot_to_mux(unsigned pot) { return (pot + 4u) & 7u; }
-static inline unsigned inputs_mux_to_pot(unsigned ch)  { return (ch  + 4u) & 7u; }
+static inline unsigned inputs_pot_to_mux(unsigned pot) { return pot & 7u; }
+static inline unsigned inputs_mux_to_pot(unsigned ch)  { return ch & 7u; }
 
 /* Rotation only. Not derivable from the switch mapping; see above. */
 extern const unsigned char inputs_mux_to_pot_rotation[8];
@@ -60,7 +48,7 @@ uint32_t inputs_get_settle_us(void);
 uint32_t inputs_get_discards(void);
 
 /* One full pass over the multiplexer. Everything is in PANEL order: index 0 and bit 0 are the
- * bottom-left knob, and button bit 0 is front-panel button 1. Both switch banks are active low
+ * top-left knob, and button bit 0 is front-panel button 1. Both switch banks are active low
  * on the wire and are inverted here, so a set bit always means "pressed". */
 typedef struct {
     uint16_t phase_a[INPUT_POTS];
diff --git a/src/hal/qt2120.c b/src/hal/qt2120.c
index 46e5f4c..50b695f 100644
--- a/src/hal/qt2120.c
+++ b/src/hal/qt2120.c
@@ -39,16 +39,11 @@
 
 #define QT2120_CHIP_ID   0x3E
 
-/* Pot -> key. The sensor's key order is not the panel's, and neither is the stock firmware's.
- *
- * The stock table (DAT_1FFE14D4) is { 5,7,6,4,3,0,1,2 }. Measured against the real panel it
- * produces a constant rotation of exactly 4 — touching the first knob lit bit 4, the second
- * bit 5, and so on, wrapping. Four is half of eight and the knobs are in two rows of four, so
- * that rotation is a row swap: stock numbers the top row first, while the UI numbers the
- * bottom row first. Apply that row swap here so touch, push, rotation, and displayed fields all
- * use the same physical order.
+/* Canonical top-row-first pot -> QT2120 key. This is the recovered stock table;
+ * the sensor keys themselves are not in panel order. Keep touch, push and rotation
+ * in the same logical order at the driver boundary.
  */
-const unsigned char qt2120_pot_to_key[8] = { 3, 0, 1, 2, 5, 7, 6, 4 };
+const unsigned char qt2120_pot_to_key[8] = { 5, 7, 6, 4, 3, 0, 1, 2 };
 #define pot_to_key qt2120_pot_to_key
 
 static uint8_t pot_threshold[8] = {
diff --git a/src/proto/emp.h b/src/proto/emp.h
index 197fa26..57a4787 100644
--- a/src/proto/emp.h
+++ b/src/proto/emp.h
@@ -21,7 +21,7 @@
 /* Minor may only ADD: opcodes, flag bits, trailing members inside field_len, or 0x80+ value
  * tags (docs/protocol.md 6). Both ends agree on min(host, device), so neither can send the
  * other something it has never heard of. */
-#define EMP_VERSION_MINOR    1u
+#define EMP_VERSION_MINOR    2u
 #define EMP_HEADER_BYTES     8u
 #define EMP_PREFIX_BYTES     8u          /* total_len u32 + crc32c u32, on FIRST && !LAST */
 
@@ -85,6 +85,8 @@
 #define EMP_KIND_CHOICE      2u
 #define EMP_KIND_READONLY    3u
 #define EMP_KIND_COLOR       4u
+#define EMP_KIND_ACTION      5u
+#define EMP_OP_ACTIVATE      0x59u
 
 /* Field `present` bitmask.
  *
diff --git a/src/proto/surface.c b/src/proto/surface.c
index 26d3a70..cc6b403 100644
--- a/src/proto/surface.c
+++ b/src/proto/surface.c
@@ -41,6 +41,7 @@ void emp_send(uint8_t channel, uint8_t opcode, const uint8_t *payload, uint32_t
 typedef struct { uint16_t off, len; } surf_choice_t;
 
 typedef struct {
+    uint16_t layout_flags;
     surf_field_t  fields[SURF_MAX_FIELDS];
     char          pool[SURF_STRING_POOL];
     surf_choice_t choices[SURF_MAX_CHOICES];
@@ -182,7 +183,7 @@ static int rd_string(rd_t *r, uint16_t *off_out, uint16_t *len_out)
 #define RDV_NONFINITE   3   /* read correctly, refused on its merits; value left at zero */
 
 static int rd_value(rd_t *r, uint8_t tag, uint8_t *b_out, double *n_out, uint32_t *c_out,
-                    uint8_t *color_count_out, float color_out[4])
+                    uint8_t *color_count_out, float color_out[4], char *text_out)
 {
     switch (tag) {
     case EMP_VAL_BOOL:   *b_out = rd8(r);  return RDV_STORED;
@@ -200,14 +201,16 @@ static int rd_value(rd_t *r, uint8_t tag, uint8_t *b_out, double *n_out, uint32_
     }
 
     case EMP_VAL_TEXT: {
-        /* A String. Skipped rather than pooled: nothing on this panel renders a text value, and
-         * spending pool on one would cost a label somewhere else. */
         uint16_t n = rd16(r);
         if (!rd_avail(r, n)) { r->bad = 1; return RDV_UNDECODABLE; }
+        if (text_out) {
+            uint16_t copy = n < 95u ? n : 95u;
+            while (copy < n && copy && (r->p[r->off + copy] & 0xc0u) == 0x80u) --copy;
+            memcpy(text_out, r->p + r->off, copy); text_out[copy] = 0;
+            if (copy < n) emp_diag(EMP_SEV_WARN, EMP_DIAG_STRING_TRUNCATED, 0, "text readout truncated");
+        }
         r->off += n;
-        emp_diag(EMP_SEV_INFO, EMP_DIAG_UNKNOWN_VALUE_TAG, EMP_VAL_TEXT,
-                 "text values are not rendered");
-        return RDV_SKIPPED;
+        return RDV_STORED;
     }
 
     case EMP_VAL_COLOR: {
@@ -361,7 +364,7 @@ static void desc_begin(const uint8_t *msg, uint32_t len)
 
     staging_revision = rd64(&r);
     staging_expect   = rd16(&r);
-    (void)rd16(&r);                       /* flags */
+    uint16_t layout_flags = rd16(&r);
     (void)rd32(&r);                       /* total_bytes, advisory */
 
     if (r.bad) return;
@@ -384,6 +387,7 @@ static void desc_begin(const uint8_t *msg, uint32_t len)
     /* Clear the SCRATCH arena. The live one is untouched, so the panel keeps showing the
      * descriptor it already has for the whole duration of the transfer -- and keeps it for good
      * if the transfer never completes. */
+    STAGE->layout_flags = emp_session_peer_minor() >= 2u ? layout_flags & 1u : 0u;
     STAGE->pool_used    = 0;
     STAGE->choices_used = 0;
     STAGE->field_count  = 0;
@@ -433,6 +437,9 @@ static void desc_field(const uint8_t *msg, uint32_t len)
     memset(f, 0, sizeof(*f));
     f->id           = slot;
     f->kind         = rd8(&r);
+    if (f->kind > EMP_KIND_ACTION || (f->kind == EMP_KIND_ACTION && emp_session_peer_minor() < 2u)) {
+        staging_open = 0; send_desc_request(EMP_REQ_SEQUENCE_BROKEN); return;
+    }
     f->flags        = rd8(&r);
     f->present      = rd16(&r);
     f->min          = rdf64(&r);
@@ -444,7 +451,7 @@ static void desc_field(const uint8_t *msg, uint32_t len)
 
     f->value_tag = rd8(&r);
     switch (rd_value(&r, f->value_tag, &f->boolean, &f->number, &f->choice,
-                     &f->color_count, f->color)) {
+                     &f->color_count, f->color, f->text)) {
     case RDV_STORED:
         break;
     case RDV_SKIPPED:
@@ -527,7 +534,7 @@ static void desc_field(const uint8_t *msg, uint32_t len)
         f->default_tag = rd8(&r);
         switch (rd_value(&r, f->default_tag, &f->default_boolean,
                          &f->default_number, &f->default_choice,
-                         &f->default_color_count, f->default_color)) {
+                         &f->default_color_count, f->default_color, 0)) {
         case RDV_STORED:
             break;
         case RDV_SKIPPED:
@@ -652,7 +659,8 @@ static void handle_values(const uint8_t *msg, uint32_t len)
         /* A tag we can step over costs one control its update, not the batch. A tag we cannot
          * costs the batch, because every value after it would be read from the wrong offset --
          * which is worse than dropping them, since they would be applied to real controls. */
-        int got = rd_value(&r, tag, &boolean, &number, &choice, &color_count, color);
+        char text[96] = {0};
+        int got = rd_value(&r, tag, &boolean, &number, &choice, &color_count, color, text);
         if (got == RDV_UNDECODABLE) break;
         if (r.bad) break;
 
@@ -677,6 +685,7 @@ static void handle_values(const uint8_t *msg, uint32_t len)
             under_hand = 1;
         if (under_hand && ack_edit < last_edit_seq[idx]) continue;
 
+        memcpy(f->text, text, sizeof(text));
         f->value_tag = tag;
         f->number    = number;
         f->choice    = choice;
@@ -769,7 +778,7 @@ void surf_set_number_cause(uint16_t index, double v, uint8_t cause)
 {
     if (!surf_field(index)) return;
     surf_field_t *f = &LIVE->fields[index];
-    if (f->kind == EMP_KIND_READONLY) return;      /* a read-only field is not ours to change */
+    if ((f->kind == EMP_KIND_READONLY || f->kind == EMP_KIND_ACTION)) return;      /* a read-only field is not ours to change */
 
     /* Nothing on the device should be able to produce this, but the arithmetic that reaches
      * here runs through the digit editor and a host-supplied min/max/step -- a step of zero and
@@ -789,7 +798,7 @@ void surf_set_bool_cause(uint16_t index, uint8_t v, uint8_t cause)
 {
     if (!surf_field(index)) return;
     surf_field_t *f = &LIVE->fields[index];
-    if (f->kind == EMP_KIND_READONLY) return;
+    if ((f->kind == EMP_KIND_READONLY || f->kind == EMP_KIND_ACTION)) return;
     f->boolean   = v ? 1u : 0u;
     f->value_tag = EMP_VAL_BOOL;
     send_edit(index, cause);
@@ -799,7 +808,7 @@ void surf_set_choice_cause(uint16_t index, uint32_t v, uint8_t cause)
 {
     if (!surf_field(index)) return;
     surf_field_t *f = &LIVE->fields[index];
-    if (f->kind == EMP_KIND_READONLY) return;
+    if ((f->kind == EMP_KIND_READONLY || f->kind == EMP_KIND_ACTION)) return;
     f->choice    = v;
     f->value_tag = EMP_VAL_CHOICE;
     send_edit(index, cause);
@@ -809,7 +818,7 @@ void surf_set_color_cause(uint16_t index, const float *v, uint8_t count, uint8_t
 {
     if (!surf_field(index) || !v || (count != 3u && count != 4u)) return;
     surf_field_t *f = &LIVE->fields[index];
-    if (f->kind == EMP_KIND_READONLY) return;
+    if ((f->kind == EMP_KIND_READONLY || f->kind == EMP_KIND_ACTION)) return;
     for (uint8_t i = 0; i < count; i++) if (!finite_f32(v[i])) return;
     memcpy(f->color, v, (uint32_t)count * sizeof(float));
     for (uint8_t i = count; i < 4u; i++) f->color[i] = 0.0f;
@@ -1047,3 +1056,37 @@ void surf_demo_choice_descriptor(uint16_t slot, const char *field_label,
     a->occupied = slot_bit(slot);
     a->revision = 0xDE31;
 }
+
+/* EMP/1.2 action presses are events, never a locally edited persistent value. */
+void surf_activate(uint16_t slot)
+{
+    static uint32_t activation_seq;
+    const surf_field_t *field = surf_field(slot);
+    if (!field || field->kind != EMP_KIND_ACTION || emp_session_peer_minor() < 2u) return;
+    uint8_t buf[14]; wr_t w; wr_init(&w, buf, sizeof(buf));
+    wr64(&w, LIVE->revision); wr32(&w, ++activation_seq); wr16(&w, slot);
+    emp_send(EMP_CH_INPUT, EMP_OP_ACTIVATE, buf, w.len);
+}
+
+int surf_operator_layout(void) { return (LIVE->layout_flags & 1u) != 0; }
+
+void surf_demo_operator(void)
+{
+    surf_demo_descriptor();
+    LIVE->layout_flags = 1;
+    const char *labels[] = {"Axis", "Step / rev", "Speed", "Position", "Jog -", "Jog +", "Home", "Stop"};
+    LIVE->pool_used = 0;
+    for (unsigned i = 0; i < 8; ++i) {
+        surf_field_t *f = &LIVE->fields[i];
+        f->label_off = LIVE->pool_used; f->label_len = (uint16_t)strlen(labels[i]);
+        memcpy(LIVE->pool + LIVE->pool_used, labels[i], f->label_len); LIVE->pool_used += f->label_len;
+        f->kind = i >= 4 ? EMP_KIND_ACTION : EMP_KIND_NUMBER;
+        f->value_tag = EMP_VAL_NUMBER; f->precision = 4; f->present = EMP_PRESENT_PRECISION;
+    }
+    LIVE->fields[0].value_tag = EMP_VAL_TEXT; strcpy(LIVE->fields[0].text, "Axis A");
+    LIVE->fields[1].number = 0.01;
+    LIVE->fields[2].number = 24000; LIVE->fields[2].precision = 0;
+    LIVE->fields[3].kind = EMP_KIND_READONLY; LIVE->fields[3].value_tag = EMP_VAL_TEXT;
+    strcpy(LIVE->fields[3].text, "0.1250");
+    ui_state_surface_changed();
+}
diff --git a/src/proto/surface.h b/src/proto/surface.h
index 4262555..059ec4d 100644
--- a/src/proto/surface.h
+++ b/src/proto/surface.h
@@ -37,6 +37,7 @@ typedef struct {
 
     double   min, max, step;
 
+    char text[96]; /* bounded UTF-8 readout, independent of descriptor label pool */
     uint8_t  value_tag;
     double   number;
     uint32_t choice;
@@ -62,6 +63,7 @@ typedef struct {
 } surf_field_t;
 
 void surf_init(void);
+int surf_operator_layout(void);
 
 /* Handle one surface-channel message. Returns nothing: per the design rule, protocol problems
  * become DIAG and DESC_REQUEST on the wire, never an error returned to a caller. */
@@ -115,6 +117,7 @@ void surf_send_delta(uint16_t index, int32_t detents);
 
 /* Focus, reported per lane. `index` < 0 means nothing is focused. */
 void surf_send_focus(int32_t index, uint16_t touch_mask);
+void surf_activate(uint16_t slot);
 void surf_on_push(unsigned pot, int pressed);
 void surf_on_touch(uint16_t touch_mask);
 void surf_on_button(unsigned button, int pressed);
@@ -122,6 +125,7 @@ void surf_on_button(unsigned button, int pressed);
 /* A locally-built descriptor for judging layout with no host attached. Deliberately awkward:
  * a long label, a negative value, a toggle, and a field with no bounds. */
 void surf_demo_descriptor(void);
+void surf_demo_operator(void);
 /* A single Choice field used by simulator layout scenes. Kept separate so the ordinary demo
  * descriptor remains stable while 3/5/7-slot and mirrored drill layouts are exercised. */
 void surf_demo_choice_descriptor(uint16_t slot, const char *field_label,
diff --git a/tests/desc_tests.c b/tests/desc_tests.c
index effc383..63d5eed 100644
--- a/tests/desc_tests.c
+++ b/tests/desc_tests.c
@@ -204,6 +204,53 @@ static int good_is_intact(void)
 
 /* ------------------------------------------------------------------ tests */
 
+static void test_small_pages_use_bottom_row(void)
+{
+    int ok = 1;
+    for (uint16_t count = 1; count <= 8; count++) {
+        xfer_t x;
+        surf_init(); ui_state_init(); sim_wire_reset();
+        xfer_begin(&x, 100 + count, count);
+        for (uint16_t i = 0; i < count; i++) xfer_number(&x, i, i, "Ordered", 0.25);
+        xfer_end(&x, 0, 0);
+        unsigned first = count <= 4 ? 4 : 0;
+        for (unsigned knob = 0; knob < 8; knob++) {
+            int32_t expected = knob >= first && knob < first + count ? (int32_t)(knob - first) : -1;
+            ok = ok && ui_state_field_for(knob) == expected;
+        }
+        if (count <= 4) {
+            ok = ok && !ui_state_rotate(0, 4, 10) && surf_field(0)->number == 0.25;
+            ui_state_push(4, 1, 11); ui_state_push(4, 0, 12);
+            ok = ok && ui_state()->focused == 0 && ui_state()->digit_top
+                    && ui_digit_row_first() == 0 && ui_cell_row_first() == 4;
+            const sim_wire_msg_t *focus = sim_wire_last(EMP_OP_FOCUS);
+            ok = ok && focus && focus->payload[9] == 0; /* ID remains zero, not physical knob 4. */
+            ui_state_push(0, 1, 13); ui_state_push(0, 0, 14);
+            ok = ok && ui_state()->focused < 0;
+            (void)ui_state_rotate(4, 4, 15);
+            const sim_wire_msg_t *edit = sim_wire_last(EMP_OP_EDIT);
+            ok = ok && surf_field(0)->number != 0.25 && edit && edit->payload[12] == 0;
+        }
+    }
+    check("1-4 controls use lower row; 5-8 use reading order; input IDs stay stable", ok);
+}
+
+static void test_sparse_small_pages_keep_order_and_ids(void)
+{
+    xfer_t x;
+    surf_init(); ui_state_init(); sim_wire_reset();
+    xfer_begin(&x, 110, 4);
+    const uint16_t ids[] = { 1, 3, 7, 9 };
+    for (uint16_t i = 0; i < 4; i++) xfer_number(&x, i, ids[i], "Sparse", 0.25);
+    xfer_end(&x, 0, 0);
+    int ok = ui_state_field_for(4) == 1 && ui_state_field_for(5) == 3
+          && ui_state_field_for(6) == 7 && ui_state_field_for(7) < 0;
+    ok = ok && ui_state_reveal(9) && ui_state_field_for(4) == 9 && ui_state_field_for(5) < 0;
+    const sim_wire_msg_t *screen = sim_wire_last(EMP_OP_SCREEN);
+    ok = ok && screen && screen->payload[8] == 8 && screen->payload[10] == 2;
+    check("sparse short pages pack left-to-right without changing IDs or page reports", ok);
+}
+
 static void test_round_trip(void)
 {
     load_good();
@@ -483,7 +530,8 @@ static void check_skippable(const char *name, uint8_t tag, const uint8_t *body,
           && f && f->id == 0
           && f->unit_len == 2                       /* the unit followed the label, and landed */
           /* Normalised to something renderable, so nothing downstream has to know the tag. */
-          && f->value_tag == EMP_VAL_NUMBER;
+          && f->value_tag == (tag == EMP_VAL_TEXT ? EMP_VAL_TEXT : EMP_VAL_NUMBER)
+          && (tag != EMP_VAL_TEXT || strcmp(f->text, "abc") == 0);
 
     check(name, ok);
 }
@@ -499,10 +547,47 @@ static void test_extension_tag_is_skipped(void)
 static void test_text_tag_is_skipped(void)
 {
     static const uint8_t body[] = { 0x03, 0x00, 'a', 'b', 'c' };
-    check_skippable("a Text value is stepped over, not misread", EMP_VAL_TEXT,
+    check_skippable("a Text value is retained without shifting the next field", EMP_VAL_TEXT,
                     body, (uint16_t)sizeof(body));
 }
 
+static void test_action_press_only(void)
+{
+    surf_init(); surf_demo_descriptor();
+    surf_field_t *field = (surf_field_t *)surf_field(0);
+    field->kind = EMP_KIND_ACTION;
+    field->present = 0;
+    ui_state_surface_changed();
+    sim_wire_reset();
+    ui_state_rotate(0, 10, 1);
+    check("action ignores rotation", sim_wire_last(EMP_OP_ACTIVATE) == 0);
+    ui_state_push(0, 1, 2);
+    const sim_wire_msg_t *m = sim_wire_last(EMP_OP_ACTIVATE);
+    check("action press emits revision/sequence/id", m && m->len == 14u);
+    unsigned before = sim_wire_count();
+    ui_state_push(0, 1, 3);
+    ui_state_push(0, 0, 4);
+    check("action held/released never repeats", sim_wire_count() == before);
+    ui_state_push(0, 1, 5);
+    check("next physical press executes again", sim_wire_count() == before + 1u);
+    ui_state_push(0, 0, 6);
+}
+
+static void test_operator_keeps_action_row(void)
+{
+    surf_init();
+    surf_demo_operator(); ui_state_init(); ui_state_surface_changed();
+    check("operator layout opted in", surf_operator_layout());
+    ui_state_push(4, 1, 10); ui_state_push(4, 0, 11);
+    check("operator setting never drills", ui_state()->focused < 0);
+    sim_wire_reset();
+    ui_state_push(3, 1, 12); ui_state_push(3, 0, 13); /* physical top-right */
+    const sim_wire_msg_t *m = sim_wire_last(EMP_OP_ACTIVATE);
+    check("operator stop retains its physical slot", m && m->len == 14 && m->payload[12] == 7);
+    ui_state_push(7, 1, 14); ui_state_push(7, 0, 15); /* bottom-right More */
+    check("operator More retains bottom-right knob", surf_page() == 8);
+}
+
 static void test_color_tag_is_native(void)
 {
     static const uint8_t body[] = { 0x03,
@@ -787,6 +872,8 @@ int desc_run_selftests(desc_report_fn report)
     failures  = 0;
     report_fn = report;
 
+    test_small_pages_use_bottom_row();
+    test_sparse_small_pages_keep_order_and_ids();
     test_round_trip();
     test_sparse_slots_preserve_holes_and_pages();
     test_duplicate_and_out_of_range_slots_keep_live();
@@ -804,6 +891,8 @@ int desc_run_selftests(desc_report_fn report)
 
     test_extension_tag_is_skipped();
     test_text_tag_is_skipped();
+    test_action_press_only();
+    test_operator_keeps_action_row();
     test_color_tag_is_native();
     test_bad_colors_keep_live_descriptor();
     test_reserved_tag_abandons_the_record();
diff --git a/tests/proto_tests.c b/tests/proto_tests.c
index 0a3dd8f..f83b31d 100644
--- a/tests/proto_tests.c
+++ b/tests/proto_tests.c
@@ -54,7 +54,7 @@ static int test_constants(void)
     CHECK(EMP_MTU_HID == 56u);
     CHECK(EMP_MTU_BULK == 1016u);
     CHECK(EMP_VERSION_MAJOR == 1u);
-    CHECK(EMP_VERSION_MINOR == 1u);
+    CHECK(EMP_VERSION_MINOR == 2u);
     CHECK(EMP_FLAG_FIRST == 0x01u && EMP_FLAG_LAST == 0x02u);
     CHECK(EMP_CH_CONTROL == 0u && EMP_CH_SURFACE == 1u && EMP_CH_INPUT == 2u);
     return ok;
diff --git a/tests/touch_cal_tests.c b/tests/touch_cal_tests.c
index fe79f7e..92116a8 100644
--- a/tests/touch_cal_tests.c
+++ b/tests/touch_cal_tests.c
@@ -13,10 +13,12 @@ static qt2120_detail_t detail;
 static uint8_t thresholds[8];
 static uint8_t gains[8];
 static int failures;
+static touch_cal_rec_t saved;
+static int have_saved;
 static touch_cal_report_fn report_fn;
 
-int persist_touch_cal_read(touch_cal_rec_t *out) { (void)out; return 0; }
-int persist_touch_cal_write(const touch_cal_rec_t *rec) { (void)rec; return 0; }
+int persist_touch_cal_read(touch_cal_rec_t *out) { *out = saved; return have_saved; }
+int persist_touch_cal_write(const touch_cal_rec_t *rec) { saved = *rec; have_saved = 1; return 0; }
 uint32_t persist_touch_cal_checksum(const touch_cal_rec_t *r)
 {
     return r->magic ^ r->version ^ r->generation ^ r->valid_mask;
@@ -41,6 +43,8 @@ static void check(const char *name, int passed)
 
 static void reset_cal(void)
 {
+    have_saved = 0;
+    memset(&saved, 0, sizeof(saved));
     memset(&detail, 0, sizeof(detail));
     memset(thresholds, QT2120_THRESHOLD, sizeof(thresholds));
     memset(gains, QT2120_DEFAULT_GAIN, sizeof(gains));
@@ -147,12 +151,37 @@ static void test_retry_keeps_all_sequence_and_final_clear(void)
     check("retry advances to next batch baseline", next_baseline);
     check("retry advances from dial 1 to dial 2", next_dial);
     check("retry retains remaining batch mask", batch_kept);
+    check("new top-left calibration saves in legacy physical slot",
+          have_saved && saved.version == TOUCH_CAL_VERSION && saved.valid_mask == 0x10u
+          && saved.threshold[4] == thresholds[0] && saved.gain[4] == gains[0]);
+    uint8_t threshold = thresholds[0], gain = gains[0];
+    touch_cal_init();
+    check("saved calibration round trip retains the same physical dial",
+          touch_cal_status()->valid_mask == 1u && thresholds[0] == threshold && gains[0] == gain);
+
+}
+
+static void test_legacy_calibration_keeps_physical_dials(void)
+{
+    reset_cal();
+    have_saved = 1;
+    saved.valid_mask = 0x92u; /* old bottom-second, top-left and top-right */
+    for (unsigned p = 0; p < 8; p++) {
+        saved.threshold[p] = (uint8_t)(20 + p);
+        saved.gain[p] = (uint8_t)p;
+    }
+    touch_cal_init();
+    check("legacy calibration migrates masks and per-dial settings together",
+          touch_cal_status()->valid_mask == 0x29u && thresholds[0] == 24 && gains[0] == 4
+          && thresholds[3] == 27 && gains[3] == 7 && thresholds[5] == 21 && gains[5] == 1
+          && thresholds[1] == QT2120_THRESHOLD && thresholds[4] == QT2120_THRESHOLD);
 }
 
 int touch_cal_run_selftests(touch_cal_report_fn report)
 {
     failures = 0;
     report_fn = report;
+    test_legacy_calibration_keeps_physical_dials();
     test_clear_interval_restarts_and_advances();
     test_active_wrong_dial_still_fails();
     test_failure_preserves_the_triggering_sample();
diff --git a/tests/ui_state_tests.c b/tests/ui_state_tests.c
index e4bed20..6ae7022 100644
--- a/tests/ui_state_tests.c
+++ b/tests/ui_state_tests.c
@@ -113,6 +113,18 @@ static void test_touch_calibration_policy(void)
 
 /* ------------------------------------------------------------------ the row split */
 
+static void test_generic_slots_follow_reading_order(void)
+{
+    surf_demo_descriptor(); ui_state_init();
+    int ok = 1;
+    for (unsigned physical = 0; physical < 8; physical++)
+        ok = ok && ui_state_field_for(physical) == (int32_t)physical;
+    ui_state_button(UI_BTN_PAGE_NEXT, 1, 1000);
+    for (unsigned physical = 0; physical < 4; physical++)
+        ok = ok && ui_state_field_for(physical + UI_ROW_SPLIT) == (int32_t)(8 + physical);
+    check("full pages use reading order, short pages use bottom row", ok);
+}
+
 static void test_row_split_is_not_the_page_size(void)
 {
     /* These two numbers used to be one constant, and were equal only by coincidence of a 4+4
@@ -137,10 +149,10 @@ static void test_press_drills_and_exits(void)
     tap(0, 1000);
     ok = ok && ui_state()->focused == 0;
 
-    /* A top knob is a digit control while drilled, so pressing one means "done here". */
+    /* A bottom knob is a digit control while drilled, so pressing one means "done here". */
     tap(UI_ROW_SPLIT, 1100);
     ok = ok && ui_state()->focused < 0;
-    check("press drills in, top press exits", ok);
+    check("press drills in, bottom press exits", ok);
 }
 
 static void test_press_is_edge_not_level(void)
@@ -162,7 +174,7 @@ static void test_press_is_edge_not_level(void)
     check("press acts on the edge, not the level", ok);
 }
 
-static void test_bottom_press_moves_between_fields(void)
+static void test_top_press_moves_between_fields(void)
 {
     surf_demo_descriptor();
     ui_state_init();
@@ -170,16 +182,16 @@ static void test_bottom_press_moves_between_fields(void)
     tap(0, 1000);
     int ok = ui_state()->focused == 0;
 
-    /* Pressing a different bottom knob moves to that field rather than leaving, which is what
+    /* Pressing a different top knob moves to that field rather than leaving, which is what
      * you want when comparing two parameters. */
     tap(1, 1100);
     ok = ok && ui_state()->focused == 1;
 
     /* Pressing the SAME knob again leaves — otherwise there would be no way out from the row
-     * you are already on without reaching for the top. */
+     * you are already on without reaching for the bottom. */
     tap(1, 1200);
     ok = ok && ui_state()->focused < 0;
-    check("bottom press moves between fields, repeat exits", ok);
+    check("top press moves between fields, repeat exits", ok);
 }
 
 static void test_drill_is_symmetric(void)
@@ -187,25 +199,25 @@ static void test_drill_is_symmetric(void)
     surf_demo_descriptor();
     ui_state_init();
 
-    /* Drill a BOTTOM field: the TOP row becomes the digit editor. */
+    /* Drill a TOP field: the BOTTOM row becomes the digit editor. */
     tap(0, 1000);
     int ok = ui_state()->focused == 0
-          && ui_state()->digit_top == 1
+          && ui_state()->digit_top == 0
           && ui_digit_row_first() == UI_ROW_SPLIT
           && ui_cell_row_first() == 0;
 
-    /* And a top press exits, because the top row is what is editing digits. */
+    /* And a bottom press exits, because the bottom row is what is editing digits. */
     tap(UI_ROW_SPLIT, 1100);
     ok = ok && ui_state()->focused < 0;
 
-    /* Drill a TOP field: the BOTTOM row becomes the digit editor, mirrored. */
+    /* Drill a BOTTOM field: the TOP row becomes the digit editor, mirrored. */
     tap(UI_ROW_SPLIT, 1200);
     ok = ok && ui_state()->focused == (int32_t)UI_ROW_SPLIT
-            && ui_state()->digit_top == 0
+            && ui_state()->digit_top == 1
             && ui_digit_row_first() == 0
             && ui_cell_row_first() == UI_ROW_SPLIT;
 
-    /* Now it is a BOTTOM press that exits, for the same reason reversed. */
+    /* Now it is a TOP press that exits, for the same reason reversed. */
     tap(0, 1300);
     ok = ok && ui_state()->focused < 0;
     check("drilling is symmetric between the rows", ok);
@@ -216,7 +228,7 @@ static void test_digit_knobs_follow_the_row(void)
     surf_demo_descriptor();
     ui_state_init();
 
-    /* Drilled into a bottom field, the TOP knobs edit digits and the BOTTOM knobs edit fields. */
+    /* Drilled into a top field, the BOTTOM knobs edit digits and the TOP knobs edit fields. */
     tap(0, 1000);
     double drilled = surf_field(0)->number;
     double neighbour = surf_field(1)->number;
@@ -225,7 +237,7 @@ static void test_digit_knobs_follow_the_row(void)
     int ok = !near(surf_field(0)->number, drilled);
     ok = ok && near(surf_field(1)->number, neighbour);  /* and it did NOT edit its own field */
 
-    /* while a bottom knob still edits its own field */
+    /* while a top knob still edits its own field */
     double other = surf_field(1)->number;
     (void)ui_state_rotate(1, 4, 1200);
     ok = ok && !near(surf_field(1)->number, other);
@@ -243,7 +255,7 @@ static void test_touch_only_highlights(void)
     int ok = ui_state()->focused < 0;
 
     tap(0, 1100);
-    ui_state_touch(0x20, 1200);                       /* a finger on top knob index 1 */
+    ui_state_touch(0x20, 1200);                       /* a finger on bottom knob index 1 */
     ok = ok && ui_state()->held_digit == 1;
 
     ui_state_touch(0x00, 1300);
@@ -265,7 +277,7 @@ static void test_page_buttons(void)
 
     ui_state_button(UI_BTN_PAGE_NEXT, 1, 1000);
     ok = ok && ui_state()->page == UI_FIELDS_PER_PAGE;
-    ok = ok && ui_state_field_for(0) == (int32_t)UI_FIELDS_PER_PAGE;
+    ok = ok && ui_state_field_for(4) == (int32_t)UI_FIELDS_PER_PAGE;
     const sim_wire_msg_t *screen = sim_wire_last(EMP_OP_SCREEN);
     ok = ok && screen && screen->len == 12 && screen->payload[8] == UI_FIELDS_PER_PAGE
             && screen->payload[10] == 4u;     /* the demo's final page is short */
@@ -315,7 +327,7 @@ static void test_readonly_is_not_editable(void)
     int ok = (f != 0) && f->kind == EMP_KIND_READONLY;
     if (ok) {
         double before = f->number;
-        (void)ui_state_rotate(idx - UI_FIELDS_PER_PAGE, 5, 1100);
+        (void)ui_state_rotate((unsigned)ui_state_knob_for(idx), 5, 1100);
         ok = near(surf_field(idx)->number, before);
     }
     check("a read-only field ignores its knob", ok);
@@ -328,7 +340,7 @@ static void test_choice_steps_and_clamps(void)
     ui_state_button(UI_BTN_PAGE_NEXT, 1, 1000);
 
     uint16_t idx = 8;
-    unsigned knob = idx - UI_FIELDS_PER_PAGE;
+    unsigned knob = (unsigned)ui_state_knob_for(idx);
     const surf_field_t *f = surf_field(idx);
     int ok = (f != 0) && f->kind == EMP_KIND_CHOICE && f->choice_count == 5;
 
@@ -367,12 +379,12 @@ static void test_choice_labels(void)
     check("choice labels are stored and readable", ok);
 }
 
-static void test_top_row_edits_digits_only_when_drilled(void)
+static void test_bottom_row_edits_digits_only_when_drilled(void)
 {
     surf_demo_descriptor();
     ui_state_init();
 
-    /* Not drilled: a top knob is an ordinary field control for its own field. */
+    /* Not drilled: a bottom knob is an ordinary field control for its own field. */
     double before = surf_field(UI_ROW_SPLIT)->number;
     (void)ui_state_rotate(UI_ROW_SPLIT, 4, 1000);
     int ok = !near(surf_field(UI_ROW_SPLIT)->number, before);
@@ -384,7 +396,7 @@ static void test_top_row_edits_digits_only_when_drilled(void)
     (void)ui_state_rotate(UI_ROW_SPLIT, UI_DIGIT_STEP, 1200);
     ok = ok && near(surf_field(UI_ROW_SPLIT)->number, other);
     ok = ok && !near(surf_field(0)->number, mine);
-    check("top row edits digits only while drilled", ok);
+    check("bottom row edits digits only while drilled", ok);
 }
 
 static void test_rotation_feedback_latches_then_expires(void)
@@ -407,20 +419,20 @@ static void test_colour_interaction(void)
     surf_demo_descriptor(); ui_state_init();
     ui_state_button(UI_BTN_PAGE_NEXT, 1, 1000);
     const uint16_t idx = 11u;
-    (void)ui_state_rotate(3, -10, 1100);
+    (void)ui_state_rotate(7, -10, 1100);
     const surf_field_t *f = surf_field(idx);
     int ok = f && f->color_count == 3u
           && nearf32(f->color[0], 0.225f) && nearf32(f->color[1], 0.45f)
           && nearf32(f->color[2], 0.9f);
 
-    tap(3, 1200);
-    (void)ui_state_rotate(UI_ROW_SPLIT, 1, 1300);
+    tap(7, 1200);
+    (void)ui_state_rotate(0, 1, 1300);
     f = surf_field(idx);
     ok = ok && nearf32(f->color[0], 0.235f)
-            && !ui_state_rotate(UI_ROW_SPLIT + 3u, 1, 1310);
+            && !ui_state_rotate(3u, 1, 1310);
 
     sim_wire_reset();
-    ui_state_touch((uint16_t)(1u << UI_ROW_SPLIT), 1400);
+    ui_state_touch((uint16_t)(1u << 0), 1400);
     const sim_wire_msg_t *focus = sim_wire_last(EMP_OP_FOCUS);
     ok = ok && focus && focus->len == 14u && focus->payload[8] == 1u
             && focus->payload[9] == idx;
@@ -638,6 +650,31 @@ static void test_brightness_settings_navigation(void)
     check("three-item settings navigation clamps", ok);
 }
 
+static void test_host_navigation_preserves_local_settings(void)
+{
+    surf_demo_descriptor();
+    enter_brightness(60u);
+    (void)ui_state_rotate(0, 5, 1050);
+    uint8_t preview = ui_state()->brightness_preview;
+    int ok = ui_state_reveal(8) && ui_state()->page == 8
+          && ui_state()->mode == UI_MODE_BRIGHTNESS
+          && ui_state()->brightness_preview == preview;
+    ui_state_surface_changed();
+    ok = ok && ui_state()->mode == UI_MODE_BRIGHTNESS
+            && ui_state()->brightness_preview == preview;
+    ui_state_surface_cleared();
+    ok = ok && ui_state()->mode == UI_MODE_BRIGHTNESS
+            && ui_state()->brightness_saved == 60u
+            && ui_state()->brightness_preview == preview;
+    ui_state_button(UI_BTN_BACK, 1, 1100);
+    ok = ok && ui_state()->mode == UI_MODE_SYSTEM
+            && ui_state()->brightness_preview == 60u;
+    ui_state_button(UI_BTN_BACK, 0, 1105);
+    ui_state_button(UI_BTN_BACK, 1, 1110);
+    ok = ok && ui_state()->mode == UI_MODE_SURFACE;
+    check("host reveal, replace and disconnect preserve local settings and cancel", ok);
+}
+
 static void test_brightness_preview_cancel_and_clamps(void)
 {
     surf_demo_descriptor();
@@ -733,11 +770,12 @@ int ui_run_selftests(ui_report_fn report)
     test_roundp();
     test_touch_calibration_policy();
 
+    test_generic_slots_follow_reading_order();
     test_row_split_is_not_the_page_size();
 
     test_press_drills_and_exits();
     test_press_is_edge_not_level();
-    test_bottom_press_moves_between_fields();
+    test_top_press_moves_between_fields();
     test_touch_only_highlights();
     test_drill_is_symmetric();
     test_digit_knobs_follow_the_row();
@@ -748,7 +786,7 @@ int ui_run_selftests(ui_report_fn report)
     test_readonly_is_not_editable();
     test_choice_steps_and_clamps();
     test_choice_labels();
-    test_top_row_edits_digits_only_when_drilled();
+    test_bottom_row_edits_digits_only_when_drilled();
     test_rotation_feedback_latches_then_expires();
     test_colour_interaction();
 
@@ -760,6 +798,7 @@ int ui_run_selftests(ui_report_fn report)
     test_system_button_reboots_and_all_calibrates();
     test_calibration_phase_actions();
     test_brightness_settings_navigation();
+    test_host_navigation_preserves_local_settings();
     test_brightness_preview_cancel_and_clamps();
     test_brightness_save_result();
     test_brightness_pwm_conversion();
diff --git a/tools/deploy/flash_usb.py b/tools/deploy/flash_usb.py
index b893014..ec51d79 100644
--- a/tools/deploy/flash_usb.py
+++ b/tools/deploy/flash_usb.py
@@ -38,7 +38,7 @@ BAUD = 115200          # ignored by CDC-ACM, but pyserial wants a number
 # At 32 the line was 104 chars and would have been silently truncated, corrupting the image
 # in a way that only a CRC check at the end would catch.
 CHUNK = 24
-ROUTER_APP_NAME = "av-control-surface-router"
+ROUTER_APP_NAME = "av-frameworks-daemon"
 ROUTER_PROVIDER_ID = "av.control-surface.electra-mini-fw"
 
 
@@ -52,6 +52,8 @@ def _router_admin_urls():
     root = os.environ.get("AV_APP_REGISTRY_DIR")
     if not root:
         local = os.environ.get("LOCALAPPDATA")
+        if not local and sys.platform == "darwin":
+            local = os.path.expanduser("~/Library/Application Support")
         if not local:
             return []
         root = os.path.join(local, "AuroraVision", "av-frameworks", "launcher")
@@ -175,8 +177,11 @@ class Device:
 
     def __init__(self, port):
         sys.path.insert(0, __file__.rsplit("\\", 1)[0])
-        from rawcom import RawCom
-        self.c = RawCom(port)
+        if sys.platform == "win32":
+            from rawcom import RawCom
+            self.c = RawCom(port)
+        else:
+            self.c = serial.Serial(port, BAUD, timeout=0.05, write_timeout=2)
         self.drain()
 
     def drain(self, quiet=0.25, limit=3.0):
@@ -209,7 +214,11 @@ class Device:
         end = time.time() + timeout
         quiet_since = None
         while time.time() < end:
-            chunk = self.c.read(65536)
+            try:
+                chunk = self.c.read(65536)
+            except serial.SerialException:
+                if not expect_prompt and line == "run": break  # USB detaches on launch
+                raise
             if chunk:
                 out += chunk
                 quiet_since = None
@@ -411,7 +420,10 @@ def app_present():
     holding the device open itself."""
     try:
         sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
-        from winusb import device_paths
+        if sys.platform == "darwin":
+            from macos_usb import device_paths
+        else:
+            from winusb import device_paths
     except Exception:
         return False
     return bool(device_paths())
@@ -426,7 +438,10 @@ def app_over_winusb():
     the reboot is exactly as it was."""
     try:
         sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
-        from winusb import WinUsbDevice, device_paths
+        if sys.platform == "darwin":
+            from macos_usb import WinUsbDevice, device_paths
+        else:
+            from winusb import WinUsbDevice, device_paths
     except Exception:
         return None
     if not device_paths():
diff --git a/tools/deploy/link.py b/tools/deploy/link.py
index 8959848..8a62ac7 100644
--- a/tools/deploy/link.py
+++ b/tools/deploy/link.py
@@ -25,7 +25,10 @@ def open_app(port=None, required=True):
     """
     if not port:
         try:
-            from winusb import WinUsbDevice, device_paths
+            if sys.platform == "darwin":
+                from macos_usb import WinUsbDevice, device_paths
+            else:
+                from winusb import WinUsbDevice, device_paths
             if device_paths():
                 return WinUsbDevice()
         except Exception:
diff --git a/tools/sim/sim_main.c b/tools/sim/sim_main.c
index c78aa66..e504d12 100644
--- a/tools/sim/sim_main.c
+++ b/tools/sim/sim_main.c
@@ -19,6 +19,7 @@
 #include "ra8876.h"
 #include "font.h"
 #include "text.h"
+#include "surface.h"
 #include "ui.h"       /* UI_BOOT_FRAMES: the scene list must not carry its own copy */
 
 static uint16_t fb[LCD_WIDTH * LCD_HEIGHT];
@@ -186,6 +187,7 @@ int main(int argc, char **argv)
     }
 
     if      (strcmp(scene, "specimen") == 0) scene_specimen();
+    else if (strcmp(scene, "operator") == 0) { surf_demo_operator(); ui_render(); }
     else if (strcmp(scene, "empty")    == 0) ui_render_empty();
     else if (strcmp(scene, "focus")    == 0) ui_render_demo_focused();
     else if (strcmp(scene, "page2")    == 0) ui_render_demo_page2();
diff --git a/tools/deploy/macos_usb.py b/tools/deploy/macos_usb.py
new file mode 100644
index 0000000..d365279
--- /dev/null
+++ b/tools/deploy/macos_usb.py
@@ -0,0 +1,54 @@
+"""Native macOS vendor bulk USB, via IOKit; no driver installation required."""
+import ctypes as c
+from pathlib import Path
+import subprocess
+import time
+
+_source = Path(__file__).with_name('usb_macos.c')
+_library = _source.parents[2] / 'build' / 'host' / 'electra_usb.dylib'
+_library.parent.mkdir(parents=True, exist_ok=True)
+if not _library.exists() or _library.stat().st_mtime < _source.stat().st_mtime:
+    subprocess.run(['cc', '-dynamiclib', '-Wno-deprecated-declarations', str(_source),
+                    '-framework', 'IOKit', '-framework', 'CoreFoundation', '-o', str(_library)], check=True)
+lib = c.CDLL(str(_library))
+lib.electra_usb_list.argtypes = [c.POINTER(c.c_uint64), c.c_uint, c.POINTER(c.c_uint)]
+lib.electra_usb_open.argtypes = [c.c_uint64, c.POINTER(c.c_void_p)]
+lib.electra_usb_close.argtypes = [c.c_void_p]
+lib.electra_usb_read.argtypes = [c.c_void_p, c.c_void_p, c.POINTER(c.c_uint32), c.c_uint32]
+lib.electra_usb_write.argtypes = [c.c_void_p, c.c_void_p, c.c_uint32]
+
+def check(code):
+    if code: raise OSError(f'Electra IOKit error 0x{code & 0xffffffff:08x}')
+
+def device_paths():
+    ids = (c.c_uint64 * 64)(); count = c.c_uint()
+    check(lib.electra_usb_list(ids, 64, c.byref(count)))
+    return [str(ids[i]) for i in range(count.value)]
+
+class WinUsbDevice:
+    """Compatible deployment-tool interface; transport is IOKit on macOS."""
+    def __init__(self, path=None, read_timeout_ms=60, **kwargs):
+        self.handle = c.c_void_p(); self.timeout_ms = read_timeout_ms
+        self.c = self; self.path = path or device_paths()[0]
+        check(lib.electra_usb_open(int(path or device_paths()[0]), c.byref(self.handle)))
+    def drain(self, rounds=64):
+        for _ in range(rounds):
+            if not self.read(): break
+    def write(self, data):
+        check(lib.electra_usb_write(self.handle, data, len(data)))
+        return len(data)
+    def read(self, size=4096):
+        buf = c.create_string_buffer(size); n = c.c_uint32(size)
+        check(lib.electra_usb_read(self.handle, buf, c.byref(n), self.timeout_ms))
+        return buf.raw[:n.value]
+    def cmd(self, line, wait=0.2):
+        self.write((line+'\r').encode()); time.sleep(wait)
+        result = b''
+        for _ in range(20):
+            chunk = self.read()
+            if not chunk: break
+            result += chunk
+        return result.decode('latin1')
+    def close(self):
+        if self.handle:
+            lib.electra_usb_close(self.handle); self.handle = c.c_void_p()
diff --git a/tools/deploy/usb_macos.c b/tools/deploy/usb_macos.c
new file mode 100644
index 0000000..91ab018
--- /dev/null
+++ b/tools/deploy/usb_macos.c
@@ -0,0 +1,102 @@
+/* macOS vendor-bulk transport. Interface ownership stays on the provider worker. */
+#include <CoreFoundation/CoreFoundation.h>
+#include <IOKit/IOCFPlugIn.h>
+#include <IOKit/usb/IOUSBLib.h>
+#include <stdint.h>
+#include <stdlib.h>
+
+typedef struct { IOUSBInterfaceInterface300 **usb; UInt8 in_pipe, out_pipe; } ElectraUSB;
+
+static int property(io_service_t service, CFStringRef key, int expected) {
+    CFTypeRef value = IORegistryEntrySearchCFProperty(service, kIOServicePlane, key,
+        kCFAllocatorDefault, kIORegistryIterateRecursively | kIORegistryIterateParents);
+    int number = -1;
+    if (value && CFGetTypeID(value) == CFNumberGetTypeID())
+        CFNumberGetValue(value, kCFNumberIntType, &number);
+    if (value) CFRelease(value);
+    return number == expected;
+}
+int electra_usb_list(uint64_t *ids, unsigned capacity, unsigned *count) {
+    io_iterator_t iterator = 0;
+    IOReturn result = IOServiceGetMatchingServices(kIOMainPortDefault,
+        IOServiceMatching(kIOUSBInterfaceClassName), &iterator);
+    if (result) return result;
+    *count = 0;
+    io_service_t service;
+    while ((service = IOIteratorNext(iterator))) {
+        if (property(service, CFSTR("idVendor"), 0x1fc9) &&
+            property(service, CFSTR("idProduct"), 0x82d0) &&
+            property(service, CFSTR("bInterfaceClass"), 0xff) && *count < capacity) {
+            uint64_t id;
+            if (!IORegistryEntryGetRegistryEntryID(service, &id)) ids[(*count)++] = id;
+        }
+        IOObjectRelease(service);
+    }
+    IOObjectRelease(iterator);
+    return 0;
+}
+void electra_usb_close(ElectraUSB *connection) {
+    if (!connection) return;
+    if (connection->usb) {
+        (*connection->usb)->USBInterfaceClose(connection->usb);
+        (*connection->usb)->Release(connection->usb);
+    }
+    free(connection);
+}
+int electra_usb_open(uint64_t id, ElectraUSB **out) {
+    *out = NULL;
+    io_service_t service = IOServiceGetMatchingService(kIOMainPortDefault, IORegistryEntryIDMatching(id));
+    if (!service) return kIOReturnNoDevice;
+    IOCFPlugInInterface **plugin = NULL;
+    SInt32 score = 0;
+    IOReturn result = IOCreatePlugInInterfaceForService(service, kIOUSBInterfaceUserClientTypeID,
+        kIOCFPlugInInterfaceID, &plugin, &score);
+    IOObjectRelease(service);
+    if (result) return result;
+    IOUSBInterfaceInterface300 **usb = NULL;
+    HRESULT query = (*plugin)->QueryInterface(plugin,
+        CFUUIDGetUUIDBytes(kIOUSBInterfaceInterfaceID300), (LPVOID *)&usb);
+    IODestroyPlugInInterface(plugin);
+    if (query || !usb) return kIOReturnUnsupported;
+    result = (*usb)->USBInterfaceOpen(usb);
+    if (result) { (*usb)->Release(usb); return result; }
+    ElectraUSB *connection = calloc(1, sizeof(*connection));
+    if (!connection) { (*usb)->USBInterfaceClose(usb); (*usb)->Release(usb); return kIOReturnNoMemory; }
+    connection->usb = usb;
+    UInt8 endpoints = 0;
+    result = (*usb)->GetNumEndpoints(usb, &endpoints);
+    for (UInt8 pipe = 1; !result && pipe <= endpoints; ++pipe) {
+        UInt8 direction, number, transfer, interval;
+        UInt16 packet;
+        result = (*usb)->GetPipeProperties(usb, pipe, &direction, &number, &transfer, &packet, &interval);
+        if (!result && transfer == kUSBBulk && number == 1) {
+            if (direction == kUSBIn) connection->in_pipe = pipe;
+            else connection->out_pipe = pipe;
+        }
+    }
+    if (result || !connection->in_pipe || !connection->out_pipe) {
+        electra_usb_close(connection);
+        return result ? result : kIOReturnUnsupported;
+    }
+    *out = connection;
+    return 0;
+}
+int electra_usb_write(ElectraUSB *c, const void *bytes, uint32_t size) {
+    return (*c->usb)->WritePipeTO(c->usb, c->out_pipe, (void *)bytes, size, 1000, 1000);
+}
+int electra_usb_read(ElectraUSB *c, void *bytes, uint32_t *size, uint32_t timeout) {
+    /* One full-speed packet per transfer: IOKit does not report partial progress
+       on a timed-out multi-packet read. Never discard consumed stream bytes. */
+    uint32_t capacity = *size, total = 0;
+    while (total < capacity) {
+        UInt32 n = capacity - total < 64 ? capacity - total : 64;
+        IOReturn result = (*c->usb)->ReadPipeTO(c->usb, c->in_pipe,
+            (uint8_t *)bytes + total, &n, total ? 1 : timeout, total ? 1 : timeout);
+        if (result == kIOUSBTransactionTimeout || result == kIOReturnTimeout) break;
+        if (result) { *size = total; return result; }
+        total += n;
+        if (n < 64) break;
+    }
+    *size = total;
+    return 0;
+}
diff --git a/tools/host.py b/tools/host.py
new file mode 100644
index 0000000..e3fed03
--- /dev/null
+++ b/tools/host.py
@@ -0,0 +1,26 @@
+"""Portable host tests and simulator: python3 tools/host.py test|sim [scene]."""
+from pathlib import Path
+import subprocess
+import sys
+r = Path(__file__).resolve().parents[1]
+out = r / 'build' / 'host'; out.mkdir(parents=True, exist_ok=True)
+mode = sys.argv[1] if len(sys.argv) > 1 else 'test'
+common = ['src/proto/diag.c', 'src/proto/frame.c', 'src/proto/surface.c',
+          'src/app/ui_state.c', 'src/app/history.c', 'tools/sim/sim_wire.c']
+if mode == 'test':
+    sources = common + ['src/proto/txq.c', 'src/app/touch_cal.c'] + ['tests/'+x+'.c' for x in
+        ['proto_tests','ui_state_tests','touch_cal_tests','desc_tests','txq_tests','host_main']]
+else:
+    sources = common + ['tools/sim/sim_main.c', 'src/app/text.c', 'src/app/font_data.c', 'src/app/ui.c']
+exe = out / mode
+subprocess.run(['cc', '-std=c11', '-Wall', '-Wextra', '-Werror=implicit-function-declaration',
+                '-Wno-unused-parameter', '-Wno-missing-field-initializers'] +
+               ['-I'+str(r/p) for p in ['src/proto','src/app','src/hal','src/common','src/bsp','tools/sim']] +
+               [str(r/p) for p in sources] + ['-lm','-o',str(exe)], check=True)
+if mode == 'test': subprocess.run([str(exe)], check=True)
+else:
+    ppm = out/'sim.ppm'
+    subprocess.run([str(exe), sys.argv[2] if len(sys.argv)>2 else 'ui', str(ppm)], check=True)
+    from PIL import Image
+    Image.open(ppm).save(out/'sim.png')
+    print(out/'sim.png')
